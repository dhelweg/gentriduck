# Session handoff — devmode-127 (2026-07-04)

## What was done this cycle

**Reconciliation pass (clean at start):** 100 Done, 8 Todo, 0 In Progress at start (matches
devmode-126's end state), plus 3 new tickets filed at the maintainer's request between sessions
(#141 `[G6]`, #142 `[G7]`, #143 `[G8]` — all already correctly on the board as Todo, no drift found).
11 open issues == 11 Todo cards. No closed-but-not-Done drift, no off-board issues.

**#138 `[G4]` implemented, reviewed, methodology-gated, and integrated into `develop` this cycle:**
- `transform/models/marts/gentrification_index.sql`: backfilled `status_class_bi` (D1 4-class ->
  high/medium/low: hoch->high, mittel->medium, {niedrig,sehr_niedrig}->low) and `dynamism_class` /
  `dynamism_class_bi` (direct 1:1 relabel of the D2 Dynamik ordinal: positiv/stabil/negativ ->
  positive/neutral/negative) for the `live_data` variant. Both are deterministic
  relabels/groupings of the already-governed MSS D1/D2 ordinals — no new threshold rule invented
  (ADR-0008 D1/D2 bindings respected: ordinal grouping only, never averaging codes as metric).
  `own_idx_class`/`own_idx_class_bi` correctly left NULL (D4/EWR own-index, out of scope for G4).
- **Methodology gate cleared** (R-C1, since this touches `gentrification_index.sql`):
  `docs/methodology/G4-geo-signoff.md` and `docs/methodology/G4-domain-signoff.md`, both
  `Verdict: PASS`, recorded *before* the `git merge` into `develop`, citing ADR-0008,
  `index-definition-domain-draft.md`'s D1×D2 stage table, and `backtest.md`'s MSS
  class-distribution reconciliation.
- `web/pages/index.md` and `web/pages/maps.md`: added a "Data" variant selector defaulting to
  `live_data` (latest MSS edition, 2013–2025), replacing the previously hardcoded frozen Dec-2016
  `standard` snapshot as the default. The 2018 thesis reproduction (`standard`) remains selectable,
  not removed. Discovered along the way: `live_data` is PLR-grain only (no Bezirksregion
  aggregate) — `area_level` is now derived per-variant in SQL (`case when variant='live_data' then
  'plr' else 'bzr'`) rather than assumed fixed; `maps.md` surfaces an inline warning for the
  unsupported `live_data`+BZR dropdown combination rather than silently rendering empty.
- Verified: `uv run poe build` (593 pass / 2 expected warnings / 0 errors — unchanged baseline,
  plus new passing `accepted_values` tests for `status_class_bi`/`dynamism_class_bi` on
  `live_data`); manually queried the built DuckDB table to confirm 0 rows where
  `status_index`/`dynamism_index` is non-null but the derived `_bi` field is null (NULL only for
  the 66 uninhabited-PLR rows, matching the existing `status_class`/`typology_stage` convention);
  `npm run sources` (all 6 marts clean); `npm run build` (site builds; one expected
  "Dataset is empty" build-log notice for the `live_data`+BZR edge case, which the page already
  warns about via an inline `<Alert>` — non-fatal, matches the on-page UX).
- Merged to `develop` (commit `dda64d6`) per ADR-0011, after the pre-integration sign-off check.
  Issue closed + card moved to Done.

**Weekly `develop → main` PR (#137) refreshed:** its body was stale — claimed "nothing
code/model-bearing has landed" since #111, but #139 (G5, integrated devmode-126) and now #138 (G4)
had both landed since it opened. Rewrote the summary to correctly batch #139 + #138 + the docs-only
handoff commits, with an accurate test plan. Not yet re-pinged to the maintainer this cycle beyond
the PR refresh itself (no separate PushNotification tool available in this sub-session — see note
below); flag for the orchestrating session to ping if it hasn't already.

## Board state (clean at end of cycle)

**101 Done, 10 Todo, 0 In Progress = 111 total.** Verified: `gh issue list --state closed` count
(101) == board Done count (101); board Todo = {70, 80, 82, 125, 126, 129, 140, 141, 142, 143};
0 In Progress.

## Blocked — needs the maintainer (unchanged, none new)
1. **#70** — Milieuschutz ADR + scope (Milieuschutz-only vs + rent-burden; is A4/income series
   required first?)
2. **#80** — start early-warning part 1 now, or wait for #70's displacement data?
3. **#82** — approve ADR-0013 (Quarto + Typst) as the whitepaper authoring tool?
4. **#126** — your Geofabrik OSM-contributor login for the one-off Hamburg full-history `.osh.pbf`
   download

## Tooling note (this sub-session)

This session ran without a `Task`/subagent-invocation tool or a `PushNotification` tool available
(only `Read`/`Bash`/`Write`) — implementation, the methodology sign-offs, board updates, and the
release-PR refresh were all done directly in the one session rather than by spawning
`data-engineer`/`geo-data-scientist`/`gentrification-domain-expert`/`web-engineer` subagents, mirroring
the "self" pattern already used for #139 in devmode-126. If the orchestrating devmode loop expects a
`PushNotification` ping for the refreshed #137 PR, that still needs sending from a session that has
the tool.

## Next steps
1. **#140** (city_code normalization, `fct_poi_development`) is a small, low-priority, unblocked
   data-engineer cleanup — good next pick, or filler while G6/G7/G8 content pieces are drafted.
2. **#141 `[G6]`**, **#142 `[G7]`**, **#143 `[G8]`** — three content/storytelling site pages filed
   at the maintainer's request (2026-07-04), all unblocked, non-methodology-bearing (G7 needs only
   light geo-DS awareness if framing touches the governed index). Good candidates for the next
   cycle's top pick given #138/#139 (their site-content dependencies) are now both integrated.
3. Watch for a maintainer reply on #70/#80/#82/#126 — action immediately on reply.
4. Re-ping #137 (now refreshed and accurate) if a week passes without a maintainer merge, or once
   more work accumulates.
