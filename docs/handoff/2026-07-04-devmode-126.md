# Session handoff — devmode-126 (2026-07-04)

## What was done this cycle

**Reconciliation pass (clean at start):** 99 Done, 6 Todo, 0 In Progress, all open issues (#70,
#80, #82, #125, #126, #129) matched the board, no drift.

**Maintainer-reply check on #70/#80/#82/#126:** re-verified via `gh issue view --json comments` —
no new maintainer reply on any of the four since devmode-125. All still genuinely blocked (same
questions as before: #70 Milieuschutz ADR+scope, #80 scoping vs #70, #82 ADR-0013 sign-off, #126
Geofabrik login). #125 remains transitively blocked on #126. #129 remains a standing
not-yet-attachable requirement.

**New scope discovered (maintainer product walkthrough of the live site):** the homepage/maps only
ever show the frozen 2018-thesis `variant = 'standard'` (Dec-2016 snapshot), never the `live_data`
variant (R-A1, #64, spans 2013–2025) — and `fct_poi_development`/`mart_price_rent_dimension_pre2021`
are fully built but invisible except per-area on `/area-detail`. Duplicate-checked (no existing
ticket covered this) and filed two new tickets, both added to the board as Todo:

- **#138 `[G4]`** Backfill binary status/dynamism classification (`status_class_bi`/
  `dynamism_class_bi`, currently NULL) for the `live_data` variant + wire `live_data` onto the
  homepage/maps as the default. Methodology-bearing (touches `gentrification_index.sql`) — needs
  geo-DS + domain-expert sign-off before integration. Not started this cycle (higher overhead than
  #139; queued next).
- **#139 `[G5]`** Add a citywide/aggregate POI-development + price/rent overview page (pure
  presentation over existing governed marts, no methodology gate). **Picked up and completed this
  cycle** (see below).

**#139 implemented, reviewed (self), and integrated into `develop` this cycle:**
- New page `web/pages/poi-price-overview.md`: citywide POI-count trend (unioned across the two
  non-overlapping LOR vintages, 2008–2026) + latest-year category mix, and citywide estimated-rent
  trend + Bodenrichtwert average from `mart_price_rent_dimension`. Cross-linked from the homepage
  ("Explore further" section).
- **Two bugs found and fixed along the way** (both directly blocking POI/price-rent visibility, so
  fixed inline rather than deferred):
  - `web/sources/gentriduck_marts/mart_price_rent_dimension_pre2021.sql` was **missing** —
    `area-detail.md` (#133/#136) already queried this table but the Evidence source definition was
    never added when the mart landed in #136, so the area-detail price/rent section had no backing
    table. Added the missing source file (same pattern as the sibling `mart_price_rent_dimension.sql`).
  - `area-detail.md`'s `poi_trend` query filtered `city_code = 'BER'`, but `fct_poi_development`'s
    Berlin rows carry the OSM-staging lowercase `'berlin'` code — the POI-development chart was
    **silently empty for every single area** since #133 shipped. Patched to
    `city_code in ('berlin', 'BER')` as an interim fix. Filed **#140** to track the proper
    upstream normalization (data-engineer, low priority — interim fix already unblocks the visible
    bug).
- Verified: `uv run poe build` (593 pass / 2 expected warnings / 0 errors — unchanged baseline),
  `npm run sources` (all 6 marts including the newly-added `mart_price_rent_dimension_pre2021`
  process cleanly), `npm run build` (full static Evidence build succeeds, no prerender errors
  across any page).
- No methodology gate applies (pure aggregation/presentation, no new indicator/weight/normalization
  logic) — merged straight to `develop` (commit `36afe86`) per ADR-0011.
- Issue closed + card moved to Done (auto-synced by the #76 board-sync convention on `gh issue
  close`).

## Board state (clean at end of cycle)
**100 Done, 8 Todo, 0 In Progress = 108 total.** Verified: `gh issue list --state closed` count
(100) == board Done count (100); board Todo = {70, 80, 82, 125, 126, 129, 138, 140}; 0 In Progress.

## Blocked — needs the maintainer (unchanged, none new)
1. **#70** — Milieuschutz ADR + scope (Milieuschutz-only vs + rent-burden; is A4/income series
   required first?)
2. **#80** — start early-warning part 1 now, or wait for #70's displacement data?
3. **#82** — approve ADR-0013 (Quarto + Typst) as the whitepaper authoring tool?
4. **#126** — your Geofabrik OSM-contributor login for the one-off Hamburg full-history `.osh.pbf`
   download

## Release PR
**#137 (`develop → main`)** — currently docs-only handoff commits (opened 2026-07-04 after #111
merged). Now also carries #139's code (36afe86) since it's on `develop`. Not yet due for a re-ping
this cycle (opened same day); will refresh the body + re-ping once either a week has passed or
enough integrated work accumulates.

## Next steps
1. **#138 `[G4]`** is next up: backfill `status_class_bi`/`dynamism_class_bi` for `live_data`
   (methodology-bearing — geo-DS + domain-expert sign-off required before integration), then wire
   `live_data` as the homepage/maps default.
2. Watch for a maintainer reply on #70/#80/#82/#126 — action immediately on reply.
3. **#140** (city_code normalization) is a small, low-priority, unblocked data-engineer cleanup —
   good filler if #138 stalls or needs sign-off turnaround time.
4. Refresh/re-ping #137 once due (weekly cadence from 2026-07-04, or once more work lands).
