import{s as wa,d as o,w as ba,i as l,a as Te,b as C,c as k,h as va,e as L,f as Qe,g as I,r as ga,t as O,j as x,k as M,u as H,l as sa,m as ya,o as ka,n as xa,p as $a,q as le}from"../chunks/scheduler.BopPEjhc.js";import{S as Ea,i as Ba,d as B,t as v,a as f,c as Re,m as z,b as R,e as S,g as Se}from"../chunks/index.CYkVJg6_.js";import{F as za}from"../chunks/FooterNav.Q7MmbWm3.js";import{H as Ra}from"../chunks/Hero.CRoRGI02.js";import{D as Xe,C as X}from"../chunks/VennDiagram.svelte_svelte_type_style_lang.DrVUZ76A.js";import{b as ze,w as Sa}from"../chunks/entry.BMmpG6A7.js";import{A as ea}from"../chunks/Alert.BO8kFSQK.js";import{e as La,s as Ma,Q as Fe,p as Ca,a as oa,r as la,C as qa}from"../chunks/inferColumnTypes.B1nNps41.js";import{h as fe}from"../chunks/setTrackProxy.Cyfckp0w.js";import{D as fa,a as Pe}from"../chunks/Dropdown.BxlIFH-r.js";import{p as Aa}from"../chunks/stores.Ceyp10jj.js";import{Q as We}from"../chunks/QueryViewer.CNDqAaoE.js";import{A as Na}from"../chunks/AreaMap.w9kcpT6D.js";import{p as Da}from"../chunks/profile.BW8tN6E9.js";function Ta(d){let a,n=D.title+"",e;return{c(){a=M("h1"),e=H(n),this.h()},l(r){a=L(r,"H1",{class:!0});var i=ga(a);e=O(i,n),i.forEach(o),this.h()},h(){C(a,"class","title")},m(r,i){l(r,a,i),Te(a,e)},p:le,d(r){r&&o(a)}}}function Pa(d){return{c(){this.h()},l(a){this.h()},h(){document.title="Evidence"},m:le,p:le,d:le}}function Ia(d){let a,n,e,r,i;return document.title=a=D.title,{c(){n=x(),e=M("meta"),r=x(),i=M("meta"),this.h()},l(m){n=k(m),e=L(m,"META",{property:!0,content:!0}),r=k(m),i=L(m,"META",{name:!0,content:!0}),this.h()},h(){var m,s;C(e,"property","og:title"),C(e,"content",((m=D.og)==null?void 0:m.title)??D.title),C(i,"name","twitter:title"),C(i,"content",((s=D.og)==null?void 0:s.title)??D.title)},m(m,s){l(m,n,s),l(m,e,s),l(m,r,s),l(m,i,s)},p(m,s){s&0&&a!==(a=D.title)&&(document.title=a)},d(m){m&&(o(n),o(e),o(r),o(i))}}}function Ua(d){var i,m;let a,n,e=(D.description||((i=D.og)==null?void 0:i.description))&&ja(),r=((m=D.og)==null?void 0:m.image)&&Oa();return{c(){e&&e.c(),a=x(),r&&r.c(),n=Qe()},l(s){e&&e.l(s),a=k(s),r&&r.l(s),n=Qe()},m(s,w){e&&e.m(s,w),l(s,a,w),r&&r.m(s,w),l(s,n,w)},p(s,w){var c,b;(D.description||(c=D.og)!=null&&c.description)&&e.p(s,w),(b=D.og)!=null&&b.image&&r.p(s,w)},d(s){s&&(o(a),o(n)),e&&e.d(s),r&&r.d(s)}}}function ja(d){let a,n,e,r,i;return{c(){a=M("meta"),n=x(),e=M("meta"),r=x(),i=M("meta"),this.h()},l(m){a=L(m,"META",{name:!0,content:!0}),n=k(m),e=L(m,"META",{property:!0,content:!0}),r=k(m),i=L(m,"META",{name:!0,content:!0}),this.h()},h(){var m,s,w;C(a,"name","description"),C(a,"content",D.description??((m=D.og)==null?void 0:m.description)),C(e,"property","og:description"),C(e,"content",((s=D.og)==null?void 0:s.description)??D.description),C(i,"name","twitter:description"),C(i,"content",((w=D.og)==null?void 0:w.description)??D.description)},m(m,s){l(m,a,s),l(m,n,s),l(m,e,s),l(m,r,s),l(m,i,s)},p:le,d(m){m&&(o(a),o(n),o(e),o(r),o(i))}}}function Oa(d){let a,n,e;return{c(){a=M("meta"),n=x(),e=M("meta"),this.h()},l(r){a=L(r,"META",{property:!0,content:!0}),n=k(r),e=L(r,"META",{name:!0,content:!0}),this.h()},h(){var r,i;C(a,"property","og:image"),C(a,"content",oa((r=D.og)==null?void 0:r.image)),C(e,"name","twitter:image"),C(e,"content",oa((i=D.og)==null?void 0:i.image))},m(r,i){l(r,a,i),l(r,n,i),l(r,e,i)},p:le,d(r){r&&(o(a),o(n),o(e))}}}function Ha(d){let a,n="How to read the map:",e,r,i='"Gentrification stage"',m,s,w='"Social status"',c,b,h='"Dynamism"',E,g,p="more deprived",P,y,A="faster",U,u,q="negative",he,V,ee="higher",Z,j,we="index definition",J,te,ne="methodology & data sources",ae;return{c(){a=M("b"),a.textContent=n,e=H(" The "),r=M("b"),r.textContent=i,m=H(` option is the easiest to read at a
  glance — it colours each area by one of six plain-language stages (red = highest pressure /
  earliest displacement risk, blue = most stable), no decoder needed — hover any area for its name
  and value. At the Planungsraum (PLR) level, the `),s=M("b"),s.textContent=w,c=H(" and "),b=M("b"),b.textContent=h,E=H(`
  options are also offered, showing the raw ordinal inputs behind that stage (not offered as a map
  colour at Bezirk/PGR/Ortsteil grain — see the Indicator note there for why): "Social status" is
  ordinal — higher shading means `),g=M("b"),g.textContent=p,P=H(`, not more prosperous. "Dynamism" — higher
  means the area's status is improving `),y=M("b"),y.textContent=A,U=H(". A "),u=M("b"),u.textContent=q,he=H(` pressure trend (see the
  table below the map) means `),V=M("b"),V.textContent=ee,Z=H(` gentrification pressure. See the
  `),j=M("a"),j.textContent=we,J=H(`
  for the full methodology, or the `),te=M("a"),te.textContent=ne,ae=H(` page for a
  plain-language walkthrough. Areas without a value (e.g. uninhabited planning areas) are drawn but
  left blank.`),this.h()},l($){a=L($,"B",{"data-svelte-h":!0}),I(a)!=="svelte-1k0da8y"&&(a.textContent=n),e=O($," The "),r=L($,"B",{"data-svelte-h":!0}),I(r)!=="svelte-1sxo7gq"&&(r.textContent=i),m=O($,` option is the easiest to read at a
  glance — it colours each area by one of six plain-language stages (red = highest pressure /
  earliest displacement risk, blue = most stable), no decoder needed — hover any area for its name
  and value. At the Planungsraum (PLR) level, the `),s=L($,"B",{"data-svelte-h":!0}),I(s)!=="svelte-1tb9m99"&&(s.textContent=w),c=O($," and "),b=L($,"B",{"data-svelte-h":!0}),I(b)!=="svelte-108pztc"&&(b.textContent=h),E=O($,`
  options are also offered, showing the raw ordinal inputs behind that stage (not offered as a map
  colour at Bezirk/PGR/Ortsteil grain — see the Indicator note there for why): "Social status" is
  ordinal — higher shading means `),g=L($,"B",{"data-svelte-h":!0}),I(g)!=="svelte-1t7i6y0"&&(g.textContent=p),P=O($,`, not more prosperous. "Dynamism" — higher
  means the area's status is improving `),y=L($,"B",{"data-svelte-h":!0}),I(y)!=="svelte-1ij4xx3"&&(y.textContent=A),U=O($,". A "),u=L($,"B",{"data-svelte-h":!0}),I(u)!=="svelte-1kmedgd"&&(u.textContent=q),he=O($,` pressure trend (see the
  table below the map) means `),V=L($,"B",{"data-svelte-h":!0}),I(V)!=="svelte-1o5dtrd"&&(V.textContent=ee),Z=O($,` gentrification pressure. See the
  `),j=L($,"A",{href:!0,"data-svelte-h":!0}),I(j)!=="svelte-1iha4fz"&&(j.textContent=we),J=O($,`
  for the full methodology, or the `),te=L($,"A",{href:!0,"data-svelte-h":!0}),I(te)!=="svelte-3zcnok"&&(te.textContent=ne),ae=O($,` page for a
  plain-language walkthrough. Areas without a value (e.g. uninhabited planning areas) are drawn but
  left blank.`),this.h()},h(){C(j,"href","https://github.com/dhelweg/gentriduck/blob/main/docs/adr/0004-data-governance-and-index-definition.md"),C(te,"href","/gentriduck/methodology")},m($,T){l($,a,T),l($,e,T),l($,r,T),l($,m,T),l($,s,T),l($,c,T),l($,b,T),l($,E,T),l($,g,T),l($,P,T),l($,y,T),l($,U,T),l($,u,T),l($,he,T),l($,V,T),l($,Z,T),l($,j,T),l($,J,T),l($,te,T),l($,ae,T)},p:le,d($){$&&(o(a),o(e),o(r),o(m),o(s),o(c),o(b),o(E),o(g),o(P),o(y),o(U),o(u),o(he),o(V),o(Z),o(j),o(J),o(te),o(ae))}}}function Ka(d){let a,n,e,r,i,m,s,w;return a=new Pe({props:{value:"plr",valueLabel:"Planungsraum (PLR) — individual neighbourhoods"}}),e=new Pe({props:{value:"bezirk",valueLabel:"Bezirk — 12 boroughs"}}),i=new Pe({props:{value:"pgr",valueLabel:"Prognoseraum (PGR) — ~58 mid-level areas"}}),s=new Pe({props:{value:"ortsteil",valueLabel:"Ortsteil — ~97 traditional neighbourhoods (approximate assignment, see caveats)"}}),{c(){S(a.$$.fragment),n=x(),S(e.$$.fragment),r=x(),S(i.$$.fragment),m=x(),S(s.$$.fragment)},l(c){R(a.$$.fragment,c),n=k(c),R(e.$$.fragment,c),r=k(c),R(i.$$.fragment,c),m=k(c),R(s.$$.fragment,c)},m(c,b){z(a,c,b),l(c,n,b),z(e,c,b),l(c,r,b),z(i,c,b),l(c,m,b),z(s,c,b),w=!0},p:le,i(c){w||(f(a.$$.fragment,c),f(e.$$.fragment,c),f(i.$$.fragment,c),f(s.$$.fragment,c),w=!0)},o(c){v(a.$$.fragment,c),v(e.$$.fragment,c),v(i.$$.fragment,c),v(s.$$.fragment,c),w=!1},d(c){c&&(o(n),o(r),o(m)),B(a,c),B(e,c),B(i,c),B(s,c)}}}function ha(d){let a,n,e,r;return a=new ea({props:{status:"info",$$slots:{default:[Fa]},$$scope:{ctx:d}}}),e=new ea({props:{status:"warning",$$slots:{default:[Wa]},$$scope:{ctx:d}}}),{c(){S(a.$$.fragment),n=x(),S(e.$$.fragment)},l(i){R(a.$$.fragment,i),n=k(i),R(e.$$.fragment,i)},m(i,m){z(a,i,m),l(i,n,m),z(e,i,m),r=!0},i(i){r||(f(a.$$.fragment,i),f(e.$$.fragment,i),r=!0)},o(i){v(a.$$.fragment,i),v(e.$$.fragment,i),r=!1},d(i){i&&o(n),B(a,i),B(e,i)}}}function Fa(d){let a,n="Bezirk / PGR / Ortsteil are population-weighted rollups of the PLR-level data",e,r,i="#310 design decision",m,s,w='most widespread ("dominant") stage',c,b,h="share",E,g,p="equal",P;return{c(){a=M("b"),a.textContent=n,e=H(` — never a
  re-scored index at this grain (averaging the underlying 1-4/1-3 MSS ordinals into a single new
  category would be statistically invalid; see the
  `),r=M("a"),r.textContent=i,m=H(`). The map
  colours by the `),s=M("b"),s.textContent=w,c=H(` among that area's constituent PLRs,
  weighted by population where available — its tooltip and the tables below always show that
  stage's `),b=M("b"),b.textContent=h,E=H(` alongside it (population-weighted where possible, equal-weighted as a
  flagged fallback otherwise), and the "Stage mix" table further down shows the full breakdown,
  never just the single most-widespread label. Areas with fewer than 3 PLRs with real data are
  flagged as a fragile ("small sample") reading. Where population data is missing for some or all
  of an area's constituent PLRs, that area falls back to `),g=M("b"),g.textContent=p,P=H(` weighting rather than true
  population weighting — flagged per-area as "population data incomplete" in the table and map
  tooltip below.`),this.h()},l(y){a=L(y,"B",{"data-svelte-h":!0}),I(a)!=="svelte-8h6csv"&&(a.textContent=n),e=O(y,` — never a
  re-scored index at this grain (averaging the underlying 1-4/1-3 MSS ordinals into a single new
  category would be statistically invalid; see the
  `),r=L(y,"A",{href:!0,"data-svelte-h":!0}),I(r)!=="svelte-vym2cu"&&(r.textContent=i),m=O(y,`). The map
  colours by the `),s=L(y,"B",{"data-svelte-h":!0}),I(s)!=="svelte-1d2azc6"&&(s.textContent=w),c=O(y,` among that area's constituent PLRs,
  weighted by population where available — its tooltip and the tables below always show that
  stage's `),b=L(y,"B",{"data-svelte-h":!0}),I(b)!=="svelte-dsjmq7"&&(b.textContent=h),E=O(y,` alongside it (population-weighted where possible, equal-weighted as a
  flagged fallback otherwise), and the "Stage mix" table further down shows the full breakdown,
  never just the single most-widespread label. Areas with fewer than 3 PLRs with real data are
  flagged as a fragile ("small sample") reading. Where population data is missing for some or all
  of an area's constituent PLRs, that area falls back to `),g=L(y,"B",{"data-svelte-h":!0}),I(g)!=="svelte-12wzsvk"&&(g.textContent=p),P=O(y,` weighting rather than true
  population weighting — flagged per-area as "population data incomplete" in the table and map
  tooltip below.`),this.h()},h(){C(r,"href","https://github.com/dhelweg/gentriduck/issues/310")},m(y,A){l(y,a,A),l(y,e,A),l(y,r,A),l(y,m,A),l(y,s,A),l(y,c,A),l(y,b,A),l(y,E,A),l(y,g,A),l(y,P,A)},p:le,d(y){y&&(o(a),o(e),o(r),o(m),o(s),o(c),o(b),o(E),o(g),o(P))}}}function Wa(d){let a,n='A "Stable, established" reading at this grain is not evidence that pressure is absent.',e,r,i=`least
  acute`,m;return{c(){a=M("b"),a.textContent=n,e=H(`
  Plurality voting resolves an area to whichever stage is most widespread among its constituent
  PLRs — and because "Stable, established" is, by construction, the most common and `),r=M("b"),r.textContent=i,m=H(` stage citywide, coarser levels are systematically biased toward reading as calm, not
  toward neutral noise. An area whose map colour reads "Stable, established" can still contain
  neighbourhoods under active gentrification pressure; this map alone cannot show you whether it
  does. The share of the area in an acute-pressure stage (residents where population data exists,
  otherwise constituent areas — in the tooltip and table below) and the Planungsraum (PLR) level —
  the finest grain this site publishes — are where that pressure is actually locatable.`)},l(s){a=L(s,"B",{"data-svelte-h":!0}),I(a)!=="svelte-uzid1v"&&(a.textContent=n),e=O(s,`
  Plurality voting resolves an area to whichever stage is most widespread among its constituent
  PLRs — and because "Stable, established" is, by construction, the most common and `),r=L(s,"B",{"data-svelte-h":!0}),I(r)!=="svelte-r2ff7v"&&(r.textContent=i),m=O(s,` stage citywide, coarser levels are systematically biased toward reading as calm, not
  toward neutral noise. An area whose map colour reads "Stable, established" can still contain
  neighbourhoods under active gentrification pressure; this map alone cannot show you whether it
  does. The share of the area in an acute-pressure stage (residents where population data exists,
  otherwise constituent areas — in the tooltip and table below) and the Planungsraum (PLR) level —
  the finest grain this site publishes — are where that pressure is actually locatable.`)},m(s,w){l(s,a,w),l(s,e,w),l(s,r,w),l(s,m,w)},p:le,d(s){s&&(o(a),o(e),o(r),o(m))}}}function Ya(d){let a,n=`<b>Indicator: Gentrification stage</b> — the only map colour offered at this area level. A
  population-weighted <i>mean</i> of the Social status / Dynamism ordinals is not offered as a
  coloured, ranked map indicator at Bezirk/PGR/Ortsteil grain (averaging discrete ordinal classes
  into a coarse-grain score and colouring or ranking by it is a documented methodology red line
  for this project — see the <a href="/gentriduck/methodology">methodology &amp; data sources</a> page); those
  raw ordinals remain available, clearly labelled as a mean ordinal class (mean rank),
  population-weighted where population data is complete and equal-weighted otherwise, in
  the table below and as a real map colour at the Planungsraum (PLR) level above.`;return{c(){a=M("p"),a.innerHTML=n,this.h()},l(e){a=L(e,"P",{class:!0,"data-svelte-h":!0}),I(a)!=="svelte-zteaav"&&(a.innerHTML=n),this.h()},h(){C(a,"class","text-sm mb-2")},m(e,r){l(e,a,r)},i:le,o:le,d(e){e&&o(a)}}}function Ga(d){let a,n;return a=new fa({props:{name:"indicator",title:"Indicator",defaultValue:"status_class",$$slots:{default:[Za]},$$scope:{ctx:d}}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function Za(d){let a,n,e,r,i,m;return a=new Pe({props:{value:"status_class",valueLabel:"Gentrification stage — plain-language, colour-coded"}}),e=new Pe({props:{value:"status_index",valueLabel:"Social status — how deprived or affluent (current snapshot)"}}),i=new Pe({props:{value:"dynamism_index",valueLabel:"Dynamism — how fast that status is changing"}}),{c(){S(a.$$.fragment),n=x(),S(e.$$.fragment),r=x(),S(i.$$.fragment)},l(s){R(a.$$.fragment,s),n=k(s),R(e.$$.fragment,s),r=k(s),R(i.$$.fragment,s)},m(s,w){z(a,s,w),l(s,n,w),z(e,s,w),l(s,r,w),z(i,s,w),m=!0},p:le,i(s){m||(f(a.$$.fragment,s),f(e.$$.fragment,s),f(i.$$.fragment,s),m=!0)},o(s){v(a.$$.fragment,s),v(e.$$.fragment,s),v(i.$$.fragment,s),m=!1},d(s){s&&(o(n),o(r)),B(a,s),B(e,s),B(i,s)}}}function ca(d){let a,n;return a=new We({props:{queryID:"areas_plr",queryResult:d[3]}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&8&&(i.queryResult=e[3]),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function da(d){let a,n;return a=new We({props:{queryID:"bezirk_names",queryResult:d[4]}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&16&&(i.queryResult=e[4]),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function ma(d){let a,n;return a=new We({props:{queryID:"areas_rollup",queryResult:d[5]}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&32&&(i.queryResult=e[5]),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function ua(d){let a,n;return a=new We({props:{queryID:"area_table_plr",queryResult:d[6]}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&64&&(i.queryResult=e[6]),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function pa(d){let a,n;return a=new We({props:{queryID:"area_table_rollup",queryResult:d[7]}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&128&&(i.queryResult=e[7]),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function Qa(d){let a,n;return a=new Xe({props:{data:d[6],rows:"10",$$slots:{default:[Ja]},$$scope:{ctx:d}}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&64&&(i.data=e[6]),r[1]&134217728&&(i.$$scope={dirty:r,ctx:e}),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function Va(d){let a,n,e,r='<a href="#stage-mix--full-breakdown-per-area">Stage mix — full breakdown per area</a>',i,m,s=`Never rely on the most widespread stage alone: this table is the full stage-mix distribution
behind every area above — population-weighted where an area&#39;s population data is complete,
equal-weighted as a flagged fallback otherwise (see the &quot;population data incomplete&quot; column) —
including the <code class="markdown">uninhabited / no data</code> bucket, shown as a count of constituent areas, not a share
(it is excluded from the share calculation by design; see that column&#39;s own values below).`,w,c,b,h;a=new Xe({props:{data:d[7],rows:"10",$$slots:{default:[Xa]},$$scope:{ctx:d}}});let E=d[8]&&_a(d);return b=new Xe({props:{data:d[8],rows:"50",search:"true",$$slots:{default:[et]},$$scope:{ctx:d}}}),{c(){S(a.$$.fragment),n=x(),e=M("h3"),e.innerHTML=r,i=x(),m=M("p"),m.innerHTML=s,w=x(),E&&E.c(),c=x(),S(b.$$.fragment),this.h()},l(g){R(a.$$.fragment,g),n=k(g),e=L(g,"H3",{class:!0,id:!0,"data-svelte-h":!0}),I(e)!=="svelte-gqegou"&&(e.innerHTML=r),i=k(g),m=L(g,"P",{class:!0,"data-svelte-h":!0}),I(m)!=="svelte-114mt3e"&&(m.innerHTML=s),w=k(g),E&&E.l(g),c=k(g),R(b.$$.fragment,g),this.h()},h(){C(e,"class","markdown"),C(e,"id","stage-mix--full-breakdown-per-area"),C(m,"class","markdown")},m(g,p){z(a,g,p),l(g,n,p),l(g,e,p),l(g,i,p),l(g,m,p),l(g,w,p),E&&E.m(g,p),l(g,c,p),z(b,g,p),h=!0},p(g,p){const P={};p[0]&128&&(P.data=g[7]),p[1]&134217728&&(P.$$scope={dirty:p,ctx:g}),a.$set(P),g[8]?E?(E.p(g,p),p[0]&256&&f(E,1)):(E=_a(g),E.c(),f(E,1),E.m(c.parentNode,c)):E&&(Se(),v(E,1,1,()=>{E=null}),Re());const y={};p[0]&256&&(y.data=g[8]),p[1]&134217728&&(y.$$scope={dirty:p,ctx:g}),b.$set(y)},i(g){h||(f(a.$$.fragment,g),f(E),f(b.$$.fragment,g),h=!0)},o(g){v(a.$$.fragment,g),v(E),v(b.$$.fragment,g),h=!1},d(g){g&&(o(n),o(e),o(i),o(m),o(w),o(c)),B(a,g),E&&E.d(g),B(b,g)}}}function Ja(d){let a,n,e,r,i,m,s,w;return a=new X({props:{id:"area_name",title:"Area"}}),e=new X({props:{id:"status_index",title:"Social status (1=least deprived … 4=most deprived)"}}),i=new X({props:{id:"dynamism_index",title:"Speed of change (higher = faster upward change)"}}),s=new X({props:{id:"stage_label",title:"Gentrification stage"}}),{c(){S(a.$$.fragment),n=x(),S(e.$$.fragment),r=x(),S(i.$$.fragment),m=x(),S(s.$$.fragment)},l(c){R(a.$$.fragment,c),n=k(c),R(e.$$.fragment,c),r=k(c),R(i.$$.fragment,c),m=k(c),R(s.$$.fragment,c)},m(c,b){z(a,c,b),l(c,n,b),z(e,c,b),l(c,r,b),z(i,c,b),l(c,m,b),z(s,c,b),w=!0},p:le,i(c){w||(f(a.$$.fragment,c),f(e.$$.fragment,c),f(i.$$.fragment,c),f(s.$$.fragment,c),w=!0)},o(c){v(a.$$.fragment,c),v(e.$$.fragment,c),v(i.$$.fragment,c),v(s.$$.fragment,c),w=!1},d(c){c&&(o(n),o(r),o(m)),B(a,c),B(e,c),B(i,c),B(s,c)}}}function Xa(d){let a,n,e,r,i,m,s,w,c,b,h,E,g,p,P,y,A,U;return a=new X({props:{id:"area_name",title:"Area"}}),e=new X({props:{id:"status_index",title:"Social status — mean ordinal class (mean rank; 1=least deprived … 4=most deprived), population-weighted where population data is complete and equal-weighted otherwise"}}),i=new X({props:{id:"dynamism_index",title:"Speed of change — mean ordinal class (mean rank), population-weighted where population data is complete and equal-weighted otherwise"}}),s=new X({props:{id:"stage_label",title:"Most widespread gentrification stage"}}),c=new X({props:{id:"dominant_share",title:"Most widespread stage's share (population-weighted, or equal-weighted — see the incomplete-data column)",fmt:"pct0"}}),h=new X({props:{id:"acute_stage_share",title:"Share in an acute-pressure stage (Active gentrification + Early pioneer signal + Improving, vulnerable area; population-weighted, or equal-weighted — see the incomplete-data column)",fmt:"pct0"}}),g=new X({props:{id:"n_habitable_children",title:"PLRs with data"}}),P=new X({props:{id:"is_dominant_fragile",title:"Fragile (< 3 PLRs)?"}}),A=new X({props:{id:"has_incomplete_population",title:"Population data incomplete (share above is equal-weighted, not population-weighted)?"}}),{c(){S(a.$$.fragment),n=x(),S(e.$$.fragment),r=x(),S(i.$$.fragment),m=x(),S(s.$$.fragment),w=x(),S(c.$$.fragment),b=x(),S(h.$$.fragment),E=x(),S(g.$$.fragment),p=x(),S(P.$$.fragment),y=x(),S(A.$$.fragment)},l(u){R(a.$$.fragment,u),n=k(u),R(e.$$.fragment,u),r=k(u),R(i.$$.fragment,u),m=k(u),R(s.$$.fragment,u),w=k(u),R(c.$$.fragment,u),b=k(u),R(h.$$.fragment,u),E=k(u),R(g.$$.fragment,u),p=k(u),R(P.$$.fragment,u),y=k(u),R(A.$$.fragment,u)},m(u,q){z(a,u,q),l(u,n,q),z(e,u,q),l(u,r,q),z(i,u,q),l(u,m,q),z(s,u,q),l(u,w,q),z(c,u,q),l(u,b,q),z(h,u,q),l(u,E,q),z(g,u,q),l(u,p,q),z(P,u,q),l(u,y,q),z(A,u,q),U=!0},p:le,i(u){U||(f(a.$$.fragment,u),f(e.$$.fragment,u),f(i.$$.fragment,u),f(s.$$.fragment,u),f(c.$$.fragment,u),f(h.$$.fragment,u),f(g.$$.fragment,u),f(P.$$.fragment,u),f(A.$$.fragment,u),U=!0)},o(u){v(a.$$.fragment,u),v(e.$$.fragment,u),v(i.$$.fragment,u),v(s.$$.fragment,u),v(c.$$.fragment,u),v(h.$$.fragment,u),v(g.$$.fragment,u),v(P.$$.fragment,u),v(A.$$.fragment,u),U=!1},d(u){u&&(o(n),o(r),o(m),o(w),o(b),o(E),o(p),o(y)),B(a,u),B(e,u),B(i,u),B(s,u),B(c,u),B(h,u),B(g,u),B(P,u),B(A,u)}}}function _a(d){let a,n;return a=new We({props:{queryID:"area_mix_table",queryResult:d[8]}}),{c(){S(a.$$.fragment)},l(e){R(a.$$.fragment,e)},m(e,r){z(a,e,r),n=!0},p(e,r){const i={};r[0]&256&&(i.queryResult=e[8]),a.$set(i)},i(e){n||(f(a.$$.fragment,e),n=!0)},o(e){v(a.$$.fragment,e),n=!1},d(e){B(a,e)}}}function et(d){let a,n,e,r,i,m,s,w,c,b;return a=new X({props:{id:"area_name",title:"Area"}}),e=new X({props:{id:"stage_label",title:"Stage"}}),i=new X({props:{id:"stage_population_share",title:"Share (population-weighted, or equal-weighted — see last column)",fmt:"pct0"}}),s=new X({props:{id:"stage_n_children",title:"Constituent PLRs"}}),c=new X({props:{id:"has_incomplete_population",title:"Population data incomplete for this area?"}}),{c(){S(a.$$.fragment),n=x(),S(e.$$.fragment),r=x(),S(i.$$.fragment),m=x(),S(s.$$.fragment),w=x(),S(c.$$.fragment)},l(h){R(a.$$.fragment,h),n=k(h),R(e.$$.fragment,h),r=k(h),R(i.$$.fragment,h),m=k(h),R(s.$$.fragment,h),w=k(h),R(c.$$.fragment,h)},m(h,E){z(a,h,E),l(h,n,E),z(e,h,E),l(h,r,E),z(i,h,E),l(h,m,E),z(s,h,E),l(h,w,E),z(c,h,E),b=!0},p:le,i(h){b||(f(a.$$.fragment,h),f(e.$$.fragment,h),f(i.$$.fragment,h),f(s.$$.fragment,h),f(c.$$.fragment,h),b=!0)},o(h){v(a.$$.fragment,h),v(e.$$.fragment,h),v(i.$$.fragment,h),v(s.$$.fragment,h),v(c.$$.fragment,h),b=!1},d(h){h&&(o(n),o(r),o(m),o(w)),B(a,h),B(e,h),B(i,h),B(s,h),B(c,h)}}}function at(d){let a,n,e,r,i,m,s,w,c="Pick an area level and an indicator below.",b,h,E=`This page covers Berlin. See <a href="/gentriduck/hamburg/maps" class="markdown">Hamburg&#39;s own maps page</a> for the same view scoped to
Hamburg&#39;s own indicators, area levels, and observation window.`,g,p,P,y,A,U,u,q,he,V,ee,Z,j,we,J,te,ne=d[2]?"profile":"neighbourhood",ae,$,T,de,Ie='<a href="#the-numbers-behind-the-map">The numbers behind the map</a>',pe,ce,_e,re,ie,ve,se,ye=`Want to see how one specific area has changed over the years? Use the
<a href="/gentriduck/berlin/time-series" class="markdown">time series page</a>. Clicking a Planungsraum (PLR) on the map above opens its
exact neighbourhood page (<a href="https://github.com/dhelweg/gentriduck/issues/133" rel="nofollow" class="markdown">#133</a>,
<a href="https://github.com/dhelweg/gentriduck/issues/150" rel="nofollow" class="markdown">#150</a>); browsing by district still works on the
<a href="/gentriduck/berlin/area-detail" class="markdown">area detail page</a>. Want the commercial-mix (shops/cafés) view instead of the
social-status index – POI density and Offering Advantage by domain – see the
<a href="/gentriduck/berlin/poi-map" class="markdown">POI &amp; Offering Advantage map</a>. Looking for the 2018 thesis reproduction&#39;s
Bezirksregion (BZR) map instead? It now lives on
<a href="/gentriduck/thesis-recheck" class="markdown">the 2018 thesis, re-checked</a>.`,ke,me,Ue='<a href="#honest-caveats">Honest caveats</a>',Le,oe,xe=`<li class="markdown"><strong class="markdown">Social status and dynamism are ordinal, not linear.</strong> Higher social-status shading means
<strong class="markdown">more deprived</strong>, not more prosperous; a negative pressure trend means <strong class="markdown">higher</strong> gentrification
pressure — see the alert above the map and <a href="/gentriduck/methodology" class="markdown">methodology &amp; data sources</a> for the
full decoder.</li> <li class="markdown"><strong class="markdown">Bezirk / PGR / Ortsteil are population-weighted rollups, never a re-scored index.</strong> The map
colours by the <em class="markdown">most widespread</em> (&quot;dominant&quot;) stage among each area&#39;s constituent PLRs — always
shown with its share (population-weighted where possible, equal-weighted as a flagged fallback
otherwise) and the full stage mix (see the &quot;Stage mix&quot; table), never as a standalone label.
Areas with fewer than 3 PLRs contributing real data are flagged fragile.</li> <li class="markdown"><strong class="markdown">A coarse-grain reading is directionally biased toward &quot;calm&quot;, not neutral.</strong> Plurality voting
resolves an area to whichever stage is most widespread among its constituents, and the most
widespread stage is, by construction, usually the least acute one — so coarser levels
systematically understate pressure rather than randomly blurring it. An area shown as &quot;Stable,
established&quot; can still contain neighbourhoods under acute pressure; this map cannot be read as
evidence that pressure is absent anywhere in it. The share of the area in an acute-pressure stage
(residents where population data exists, otherwise constituent areas — in the tooltip and table
above) and the Planungsraum (PLR) level — the finest grain this site publishes — are where that
pressure is actually locatable.</li> <li class="markdown"><strong class="markdown">The rollup &quot;Social status&quot;/&quot;Speed of change&quot; figures are a mean ordinal class (a mean rank),
population-weighted where population data is complete and equal-weighted otherwise, not a
rescaled score.</strong> They are not offered as a map colour at Bezirk/PGR/Ortsteil grain, and the
table above no longer ranks by them, for exactly this reason — see the Indicator note above the
map.</li> <li class="markdown"><strong class="markdown">Ortsteil figures are approximate, unlike PGR/Bezirk which nest exactly.</strong> PLRs do not nest
cleanly into Ortsteile, so each Ortsteil&#39;s figures are assembled from the PLRs a dominant-overlap
rule assigns to it, not from a clean administrative nesting — the drawn polygon is the true
Ortsteil boundary, but the value describes that assigned-PLR set.</li> <li class="markdown"><strong class="markdown">&quot;Population-weighted&quot; is a best-effort weighting, not a guarantee.</strong> Where population data is
missing for some or all of an area&#39;s constituent PLRs, that area&#39;s rollup falls back to <em class="markdown">equal</em>
weighting across its constituent PLRs rather than true population weighting — flagged per-area
as &quot;population data incomplete&quot; in the table and map tooltip.</li> <li class="markdown">This map&#39;s Planungsraum (PLR) level covers Berlin&#39;s current, live data at neighbourhood detail.
The 2018 thesis&#39;s Dec-2016-snapshot reproduction, at the coarser Bezirksregion level, has its own
fixed map on <a href="/gentriduck/thesis-recheck" class="markdown">the 2018 thesis, re-checked</a>.</li> <li class="markdown">Areas without a value (e.g. uninhabited planning areas) are drawn but left blank — a blank area
is missing data, not a &quot;zero pressure&quot; reading.</li>`,$e,ue,je='<a href="#where-next">Where next</a>',Me,be,Ze=`<li class="markdown"><strong class="markdown"><a href="/gentriduck/berlin/time-series" class="markdown">Time series</a></strong> — how one specific area, or the whole city, has moved
over the years.</li> <li class="markdown"><strong class="markdown"><a href="/gentriduck/berlin/area-detail" class="markdown">Area detail</a></strong> — browse by district, or open one neighbourhood&#39;s full
profile via a map click.</li> <li class="markdown"><strong class="markdown"><a href="/gentriduck/berlin/poi-map" class="markdown">POI &amp; Offering Advantage map</a></strong> — the commercial-mix (shops/cafés) view of
the same neighbourhoods.</li> <li class="markdown"><strong class="markdown"><a href="/gentriduck/thesis-recheck" class="markdown">The 2018 thesis, re-checked</a></strong> — the fixed 2018 thesis reproduction map
(Bezirksregion, Dec 2016 snapshot) and six-hypothesis writeup.</li> <li class="markdown"><strong class="markdown"><a href="/gentriduck/methodology" class="markdown">Methodology &amp; data sources</a></strong> — what &quot;gentrification pressure&quot; and the six-stage
typology mean.</li>`,Oe,Ae,He,N,Ee,ge=typeof D<"u"&&D.title&&D.hide_title!==!0&&Ta();function Ye(t,_){return typeof D<"u"&&D.title?Ia:Pa}let Ge=Ye()(d),Be=typeof D=="object"&&Ua();m=new Ra({props:{compact:!0,eyebrow:"Chapter 3 — The Evidence",title:"Maps — gentrification pressure by area",lede:"Colours each of Berlin's neighbourhoods by its current gentrification-pressure signal, so you can see at a glance which parts of the city show the strongest or weakest pressure."}}),p=new ea({props:{status:"info",$$slots:{default:[Ha]},$$scope:{ctx:d}}}),y=new fa({props:{name:"area_level",title:"Area level",defaultValue:"plr",$$slots:{default:[Ka]},$$scope:{ctx:d}}});let Q=d[2]&&ha(d);const aa=[Ga,Ya],Ne=[];function ta(t,_){return t[2]?1:0}u=ta(d),q=Ne[u]=aa[u](d);let K=d[3]&&ca(d),F=d[4]&&da(d),W=d[5]&&ma(d);j=new Na({props:{data:d[2]?d[5]:d[3],geoJsonUrl:d[10],geoId:"area_code",areaCol:"area_code",value:d[1]==="status_class"?"stage_label":d[1],legendType:d[1]==="status_class"?"categorical":"scalar",colorPalette:d[1]==="status_class"?d[12](d[2]?d[5]:d[3]):void 0,title:"Berlin "+d[13][d[0].area_level.value]+" — "+d[11]+", latest period",startingLat:52.52,startingLong:13.405,startingZoom:9,link:"link",tooltip:d[9]}});let Y=d[6]&&ua(d),G=d[7]&&pa(d);const na=[Va,Qa],Ce=[];function ra(t,_){return t[2]?0:1}return re=ra(d),ie=Ce[re]=na[re](d),N=new za({}),{c(){ge&&ge.c(),a=x(),Ge.c(),n=M("meta"),e=M("meta"),Be&&Be.c(),r=Qe(),i=x(),S(m.$$.fragment),s=x(),w=M("p"),w.textContent=c,b=x(),h=M("p"),h.innerHTML=E,g=x(),S(p.$$.fragment),P=x(),S(y.$$.fragment),A=x(),Q&&Q.c(),U=x(),q.c(),he=x(),K&&K.c(),V=x(),F&&F.c(),ee=x(),W&&W.c(),Z=x(),S(j.$$.fragment),we=x(),J=M("p"),te=H("Click an area on the map to open its exact "),ae=H(ne),$=H(" page."),T=x(),de=M("h2"),de.innerHTML=Ie,pe=x(),Y&&Y.c(),ce=x(),G&&G.c(),_e=x(),ie.c(),ve=x(),se=M("p"),se.innerHTML=ye,ke=x(),me=M("h2"),me.innerHTML=Ue,Le=x(),oe=M("ul"),oe.innerHTML=xe,$e=x(),ue=M("h2"),ue.innerHTML=je,Me=x(),be=M("ul"),be.innerHTML=Ze,Oe=x(),Ae=M("hr"),He=x(),S(N.$$.fragment),this.h()},l(t){ge&&ge.l(t),a=k(t);const _=va("svelte-2igo1p",document.head);Ge.l(_),n=L(_,"META",{name:!0,content:!0}),e=L(_,"META",{name:!0,content:!0}),Be&&Be.l(_),r=Qe(),_.forEach(o),i=k(t),R(m.$$.fragment,t),s=k(t),w=L(t,"P",{class:!0,"data-svelte-h":!0}),I(w)!=="svelte-sy1lpd"&&(w.textContent=c),b=k(t),h=L(t,"P",{class:!0,"data-svelte-h":!0}),I(h)!=="svelte-81tax8"&&(h.innerHTML=E),g=k(t),R(p.$$.fragment,t),P=k(t),R(y.$$.fragment,t),A=k(t),Q&&Q.l(t),U=k(t),q.l(t),he=k(t),K&&K.l(t),V=k(t),F&&F.l(t),ee=k(t),W&&W.l(t),Z=k(t),R(j.$$.fragment,t),we=k(t),J=L(t,"P",{class:!0});var De=ga(J);te=O(De,"Click an area on the map to open its exact "),ae=O(De,ne),$=O(De," page."),De.forEach(o),T=k(t),de=L(t,"H2",{class:!0,id:!0,"data-svelte-h":!0}),I(de)!=="svelte-1loy64m"&&(de.innerHTML=Ie),pe=k(t),Y&&Y.l(t),ce=k(t),G&&G.l(t),_e=k(t),ie.l(t),ve=k(t),se=L(t,"P",{class:!0,"data-svelte-h":!0}),I(se)!=="svelte-sp7hpd"&&(se.innerHTML=ye),ke=k(t),me=L(t,"H2",{class:!0,id:!0,"data-svelte-h":!0}),I(me)!=="svelte-ad0syq"&&(me.innerHTML=Ue),Le=k(t),oe=L(t,"UL",{class:!0,"data-svelte-h":!0}),I(oe)!=="svelte-1cln85c"&&(oe.innerHTML=xe),$e=k(t),ue=L(t,"H2",{class:!0,id:!0,"data-svelte-h":!0}),I(ue)!=="svelte-1v8k4cs"&&(ue.innerHTML=je),Me=k(t),be=L(t,"UL",{class:!0,"data-svelte-h":!0}),I(be)!=="svelte-1ws1ci0"&&(be.innerHTML=Ze),Oe=k(t),Ae=L(t,"HR",{class:!0}),He=k(t),R(N.$$.fragment,t),this.h()},h(){C(n,"name","twitter:card"),C(n,"content","summary_large_image"),C(e,"name","twitter:site"),C(e,"content","@evidence_dev"),C(w,"class","markdown"),C(h,"class","markdown"),C(J,"class","markdown"),C(de,"class","markdown"),C(de,"id","the-numbers-behind-the-map"),C(se,"class","markdown"),C(me,"class","markdown"),C(me,"id","honest-caveats"),C(oe,"class","markdown"),C(ue,"class","markdown"),C(ue,"id","where-next"),C(be,"class","markdown"),C(Ae,"class","markdown")},m(t,_){ge&&ge.m(t,_),l(t,a,_),Ge.m(document.head,null),Te(document.head,n),Te(document.head,e),Be&&Be.m(document.head,null),Te(document.head,r),l(t,i,_),z(m,t,_),l(t,s,_),l(t,w,_),l(t,b,_),l(t,h,_),l(t,g,_),z(p,t,_),l(t,P,_),z(y,t,_),l(t,A,_),Q&&Q.m(t,_),l(t,U,_),Ne[u].m(t,_),l(t,he,_),K&&K.m(t,_),l(t,V,_),F&&F.m(t,_),l(t,ee,_),W&&W.m(t,_),l(t,Z,_),z(j,t,_),l(t,we,_),l(t,J,_),Te(J,te),Te(J,ae),Te(J,$),l(t,T,_),l(t,de,_),l(t,pe,_),Y&&Y.m(t,_),l(t,ce,_),G&&G.m(t,_),l(t,_e,_),Ce[re].m(t,_),l(t,ve,_),l(t,se,_),l(t,ke,_),l(t,me,_),l(t,Le,_),l(t,oe,_),l(t,$e,_),l(t,ue,_),l(t,Me,_),l(t,be,_),l(t,Oe,_),l(t,Ae,_),l(t,He,_),z(N,t,_),Ee=!0},p(t,_){typeof D<"u"&&D.title&&D.hide_title!==!0&&ge.p(t,_),Ge.p(t,_),typeof D=="object"&&Be.p(t,_);const De={};_[1]&134217728&&(De.$$scope={dirty:_,ctx:t}),p.$set(De);const ia={};_[1]&134217728&&(ia.$$scope={dirty:_,ctx:t}),y.$set(ia),t[2]?Q?_[0]&4&&f(Q,1):(Q=ha(t),Q.c(),f(Q,1),Q.m(U.parentNode,U)):Q&&(Se(),v(Q,1,1,()=>{Q=null}),Re());let Ve=u;u=ta(t),u!==Ve&&(Se(),v(Ne[Ve],1,1,()=>{Ne[Ve]=null}),Re(),q=Ne[u],q||(q=Ne[u]=aa[u](t),q.c()),f(q,1),q.m(he.parentNode,he)),t[3]?K?(K.p(t,_),_[0]&8&&f(K,1)):(K=ca(t),K.c(),f(K,1),K.m(V.parentNode,V)):K&&(Se(),v(K,1,1,()=>{K=null}),Re()),t[4]?F?(F.p(t,_),_[0]&16&&f(F,1)):(F=da(t),F.c(),f(F,1),F.m(ee.parentNode,ee)):F&&(Se(),v(F,1,1,()=>{F=null}),Re()),t[5]?W?(W.p(t,_),_[0]&32&&f(W,1)):(W=ma(t),W.c(),f(W,1),W.m(Z.parentNode,Z)):W&&(Se(),v(W,1,1,()=>{W=null}),Re());const qe={};_[0]&44&&(qe.data=t[2]?t[5]:t[3]),_[0]&1024&&(qe.geoJsonUrl=t[10]),_[0]&2&&(qe.value=t[1]==="status_class"?"stage_label":t[1]),_[0]&2&&(qe.legendType=t[1]==="status_class"?"categorical":"scalar"),_[0]&46&&(qe.colorPalette=t[1]==="status_class"?t[12](t[2]?t[5]:t[3]):void 0),_[0]&2049&&(qe.title="Berlin "+t[13][t[0].area_level.value]+" — "+t[11]+", latest period"),_[0]&512&&(qe.tooltip=t[9]),j.$set(qe),(!Ee||_[0]&4)&&ne!==(ne=t[2]?"profile":"neighbourhood")&&ba(ae,ne),t[6]?Y?(Y.p(t,_),_[0]&64&&f(Y,1)):(Y=ua(t),Y.c(),f(Y,1),Y.m(ce.parentNode,ce)):Y&&(Se(),v(Y,1,1,()=>{Y=null}),Re()),t[7]?G?(G.p(t,_),_[0]&128&&f(G,1)):(G=pa(t),G.c(),f(G,1),G.m(_e.parentNode,_e)):G&&(Se(),v(G,1,1,()=>{G=null}),Re());let Je=re;re=ra(t),re===Je?Ce[re].p(t,_):(Se(),v(Ce[Je],1,1,()=>{Ce[Je]=null}),Re(),ie=Ce[re],ie?ie.p(t,_):(ie=Ce[re]=na[re](t),ie.c()),f(ie,1),ie.m(ve.parentNode,ve))},i(t){Ee||(f(m.$$.fragment,t),f(p.$$.fragment,t),f(y.$$.fragment,t),f(Q),f(q),f(K),f(F),f(W),f(j.$$.fragment,t),f(Y),f(G),f(ie),f(N.$$.fragment,t),Ee=!0)},o(t){v(m.$$.fragment,t),v(p.$$.fragment,t),v(y.$$.fragment,t),v(Q),v(q),v(K),v(F),v(W),v(j.$$.fragment,t),v(Y),v(G),v(ie),v(N.$$.fragment,t),Ee=!1},d(t){t&&(o(a),o(i),o(s),o(w),o(b),o(h),o(g),o(P),o(A),o(U),o(he),o(V),o(ee),o(Z),o(we),o(J),o(T),o(de),o(pe),o(ce),o(_e),o(ve),o(se),o(ke),o(me),o(Le),o(oe),o($e),o(ue),o(Me),o(be),o(Oe),o(Ae),o(He)),ge&&ge.d(t),Ge.d(t),o(n),o(e),Be&&Be.d(t),o(r),B(m,t),B(p,t),B(y,t),Q&&Q.d(t),Ne[u].d(t),K&&K.d(t),F&&F.d(t),W&&W.d(t),B(j,t),Y&&Y.d(t),G&&G.d(t),Ce[re].d(t),B(N,t)}}}const D={title:"Maps — gentrification pressure by area"};function tt(d,a,n){let e,r,i,m,s,w,c;sa(d,Aa,N=>n(40,w=N)),sa(d,la,N=>n(45,c=N));let{data:b}=a,{data:h={},customFormattingSettings:E,__db:g,inputs:p}=b;ya(la,c="58e4d6dd5ac3a303a1d759de3fdf1f19",c);let P=La(Sa(p));ka(P.subscribe(N=>n(0,p=N))),xa(qa,{getCustomFormats:()=>E.customFormats||[]});const y=(N,Ee)=>Da(g.query,N,{query_name:Ee});Ma(y),w.params,$a(()=>!0);let A={initialData:void 0,initialError:void 0},U=fe`-- #152: stage_label is the de-jargoned, human-readable form of status_class (typology_stage
-- from int_gentrification_ts's D1xD2 matrix, ADR-0008 -- no thresholds touched here, just a
-- friendlier string). stage_sort orders rows by gentrification-pressure severity (most acute
-- first) so this data's first-occurrence order of distinct stage_label values -- what
-- \`presentStagePalette()\` above (and EvidenceMap.js's own handleLegendValues/handleFillColor)
-- keys its colour lookup on -- is deterministic and matches the "worse -> red" reading order,
-- regardless of DuckDB's natural row order. Ordering rationale (Dangschat 1988 double
-- invasion-succession cycle; Döring & Ulbricht 2016 vulnerability framework -- both cited in
-- int_gentrification_ts.sql):
--   1 active-gentrification  -- mid-status area improving fastest: gentrification in motion.
--   2 pioneer-signal         -- low-status area improving fast: earliest displacement signal.
--   3 improving-vulnerable   -- most-deprived area improving: vulnerable population, watch.
--   4 pre-gentrification     -- early/mixed signals (filtering-down or nascent upgrading).
--   5 consolidation-pressure -- already-affluent area still intensifying: lower urgency.
--   6 stable-established     -- no material status change: least pressure.
select
    city_code,
    area_code,
    area_name,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- Drill-down click-through target (#133 G1d, exact-code fix #150): /berlin/area/[code]
    -- queries fct_gentrification_change etc. on lor_2021 (current, 542-PLR) area codes, which
    -- this \`live_data\`/\`plr\` query always returns -- see #309 for why the \`standard\` variant
    -- (pre-2021 codes, no click-through) no longer appears on this page at all.
    '${ze}/berlin/area/' || area_code as link
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by stage_sort`,u=`-- #152: stage_label is the de-jargoned, human-readable form of status_class (typology_stage
-- from int_gentrification_ts's D1xD2 matrix, ADR-0008 -- no thresholds touched here, just a
-- friendlier string). stage_sort orders rows by gentrification-pressure severity (most acute
-- first) so this data's first-occurrence order of distinct stage_label values -- what
-- \`presentStagePalette()\` above (and EvidenceMap.js's own handleLegendValues/handleFillColor)
-- keys its colour lookup on -- is deterministic and matches the "worse -> red" reading order,
-- regardless of DuckDB's natural row order. Ordering rationale (Dangschat 1988 double
-- invasion-succession cycle; Döring & Ulbricht 2016 vulnerability framework -- both cited in
-- int_gentrification_ts.sql):
--   1 active-gentrification  -- mid-status area improving fastest: gentrification in motion.
--   2 pioneer-signal         -- low-status area improving fast: earliest displacement signal.
--   3 improving-vulnerable   -- most-deprived area improving: vulnerable population, watch.
--   4 pre-gentrification     -- early/mixed signals (filtering-down or nascent upgrading).
--   5 consolidation-pressure -- already-affluent area still intensifying: lower urgency.
--   6 stable-established     -- no material status change: least pressure.
select
    city_code,
    area_code,
    area_name,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- Drill-down click-through target (#133 G1d, exact-code fix #150): /berlin/area/[code]
    -- queries fct_gentrification_change etc. on lor_2021 (current, 542-PLR) area codes, which
    -- this \`live_data\`/\`plr\` query always returns -- see #309 for why the \`standard\` variant
    -- (pre-2021 codes, no click-through) no longer appears on this page at all.
    '${ze}/berlin/area/' || area_code as link
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by stage_sort`;h.areas_plr_data&&(h.areas_plr_data instanceof Error?A.initialError=h.areas_plr_data:A.initialData=h.areas_plr_data,h.areas_plr_columns&&(A.knownColumns=h.areas_plr_columns));let q,he=!1;const V=Fe.createReactive({callback:N=>{n(3,q=N)},execFn:y},{id:"areas_plr",...A});V(u,{noResolve:U,...A}),globalThis[Symbol.for("areas_plr")]={get value(){return q}};let ee={initialData:void 0,initialError:void 0},Z=fe`-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)`,j=`-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)`;h.bezirk_names_data&&(h.bezirk_names_data instanceof Error?ee.initialError=h.bezirk_names_data:ee.initialData=h.bezirk_names_data,h.bezirk_names_columns&&(ee.knownColumns=h.bezirk_names_columns));let we,J=!1;const te=Fe.createReactive({callback:N=>{n(4,we=N)},execFn:y},{id:"bezirk_names",...ee});te(j,{noResolve:Z,...ee}),globalThis[Symbol.for("bezirk_names")]={get value(){return we}};let ne={initialData:void 0,initialError:void 0},ae=fe`-- One row per rollup area (bezirk/pgr/ortsteil), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean, n_habitable_children,
-- is_dominant_fragile and has_incomplete_population are all CONSTANT across an area's stage rows
-- (see that mart's header), so any single row carries them; the row itself is not otherwise used
-- (the "Stage mix" table further down this page queries the full per-stage grain separately).
--
-- #310 review fix (MEDIUM-A): area_name falls back to the shared Bezirk-name lookup block above
-- (see that block's header comment; referenced by name in the actual \`left join\` below) wherever
-- the mart's own area_name is NULL -- every Bezirk row, since dim_area carries no bezirk-level
-- rows. At PGR/Ortsteil grain the mart's area_name is always populated, so the left join below is
-- a no-op there (no PGR/Ortsteil code ever matches a '01'..'12' Bezirk code).
--
-- NB: this comment deliberately avoids spelling out the lookup block's interpolation token in
-- prose (see the \`bezirk_names\` block's own header comment for why -- doing so would textually
-- substitute the lookup's entire compiled SQL into the middle of THIS single-line comment,
-- breaking out of the comment and corrupting the query, exactly as it would for a self-reference).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (Dangschat's invasion phase plus the Döring/Ulbricht
-- vulnerability case) from the mart's own per-(area, typology_stage) grain -- a plain aggregate
-- over already-published mix rows, no mart change, permitted under the standing #267 domain
-- decision's Recommendation 4 ("share of PLRs in active-gentrification typology" is an explicitly
-- allowed composition statistic). Filtering to one (city, level, period, variant) before grouping
-- by area_code cannot double-count, since that combination plus typology_stage is this mart's
-- natural key.
--
-- NB: the mart is SPARSE, not padded to all six stages per area (\`stage_agg\` there groups by
-- observed \`typology_stage\` only) -- so \`sum(...) filter (where typology_stage in (...))\` returns
-- NULL, not 0, for an area with real habitable children but none in an acute stage (zero matching
-- rows to sum). \`acute_stage_share_raw\` is therefore coalesced to 0 in \`base\` below, but ONLY when
-- the area has at least one habitable child -- an orphan area (n_habitable_children = 0) keeps a
-- genuine NULL, matching dominant_share's own convention.
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    ),
    base as (
        select
            m.city_code,
            m.area_code,
            coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
            m.status_index_weighted_mean as status_index,
            m.dynamism_index_weighted_mean as dynamism_index,
            -- dominant_stage as status_class kept for shape symmetry with \`areas_plr\`'s
            -- status_class column (not otherwise consumed downstream at this grain -- the map/
            -- tooltip read \`stage_label\` instead).
            m.dominant_stage as status_class,
            m.dominant_share,
            case
                when m.n_habitable_children = 0 then null
                else coalesce(a.acute_stage_share_raw, 0)
            end as acute_stage_share,
            m.n_habitable_children,
            m.is_dominant_fragile,
            m.has_incomplete_population,
            case m.dominant_stage
                when 'active-gentrification' then 'Active gentrification'
                when 'pioneer-signal' then 'Early pioneer signal'
                when 'improving-vulnerable' then 'Improving, vulnerable area'
                when 'pre-gentrification' then 'Pre-gentrification watch'
                when 'consolidation-pressure' then 'Consolidated, still intensifying'
                when 'stable-established' then 'Stable, established'
            end as stage_label,
            case m.dominant_stage
                when 'active-gentrification' then 1
                when 'pioneer-signal' then 2
                when 'improving-vulnerable' then 3
                when 'pre-gentrification' then 4
                when 'consolidation-pressure' then 5
                when 'stable-established' then 6
                else 99
            end as stage_sort,
            -- #310 review fix (MEDIUM-4): pairs the dominant-stage reading with an explicit
            -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment
            -- point 3) this must never be shown as a standalone, confident-looking label. Blank
            -- ('') rather than NULL when not fragile, so the tooltip line renders empty instead
            -- of a formatted "-" placeholder.
            case
                when m.is_dominant_fragile
                then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
                else ''
            end as fragile_note,
            -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit
            -- caveat whenever the weighting silently degraded to equal-weight for this area (see
            -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / MEDIUM-C fix: whenever this flag
            -- is true, the mart now equal-weights EVERY child in the area consistently -- never a
            -- silent partial-coverage mix of weighting schemes within the same area).
            case
                when m.has_incomplete_population
                then 'Population data incomplete for this area — equal-weighted, not population-weighted'
                else ''
            end as population_note,
            -- Rollup profile pages (all pre-existing routes): /berlin/area/<level>/<code>.
            '${ze}/berlin/area/${p.area_level.value}/' || m.area_code as link
        from gentriduck_marts.mart_area_rollup_stage_mix as m
        left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
        left join acute as a on m.area_code = a.area_code
        where m.area_level = '${p.area_level.value}'
          and m.city_code = 'BER'
          -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
          -- defends against a future second variant silently duplicating rows here.
          and m.variant = 'live_data'
          and m.period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
    )
select *
from base
order by stage_sort`,$=`-- One row per rollup area (bezirk/pgr/ortsteil), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean, n_habitable_children,
-- is_dominant_fragile and has_incomplete_population are all CONSTANT across an area's stage rows
-- (see that mart's header), so any single row carries them; the row itself is not otherwise used
-- (the "Stage mix" table further down this page queries the full per-stage grain separately).
--
-- #310 review fix (MEDIUM-A): area_name falls back to the shared Bezirk-name lookup block above
-- (see that block's header comment; referenced by name in the actual \`left join\` below) wherever
-- the mart's own area_name is NULL -- every Bezirk row, since dim_area carries no bezirk-level
-- rows. At PGR/Ortsteil grain the mart's area_name is always populated, so the left join below is
-- a no-op there (no PGR/Ortsteil code ever matches a '01'..'12' Bezirk code).
--
-- NB: this comment deliberately avoids spelling out the lookup block's interpolation token in
-- prose (see the \`bezirk_names\` block's own header comment for why -- doing so would textually
-- substitute the lookup's entire compiled SQL into the middle of THIS single-line comment,
-- breaking out of the comment and corrupting the query, exactly as it would for a self-reference).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (Dangschat's invasion phase plus the Döring/Ulbricht
-- vulnerability case) from the mart's own per-(area, typology_stage) grain -- a plain aggregate
-- over already-published mix rows, no mart change, permitted under the standing #267 domain
-- decision's Recommendation 4 ("share of PLRs in active-gentrification typology" is an explicitly
-- allowed composition statistic). Filtering to one (city, level, period, variant) before grouping
-- by area_code cannot double-count, since that combination plus typology_stage is this mart's
-- natural key.
--
-- NB: the mart is SPARSE, not padded to all six stages per area (\`stage_agg\` there groups by
-- observed \`typology_stage\` only) -- so \`sum(...) filter (where typology_stage in (...))\` returns
-- NULL, not 0, for an area with real habitable children but none in an acute stage (zero matching
-- rows to sum). \`acute_stage_share_raw\` is therefore coalesced to 0 in \`base\` below, but ONLY when
-- the area has at least one habitable child -- an orphan area (n_habitable_children = 0) keeps a
-- genuine NULL, matching dominant_share's own convention.
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    ),
    base as (
        select
            m.city_code,
            m.area_code,
            coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
            m.status_index_weighted_mean as status_index,
            m.dynamism_index_weighted_mean as dynamism_index,
            -- dominant_stage as status_class kept for shape symmetry with \`areas_plr\`'s
            -- status_class column (not otherwise consumed downstream at this grain -- the map/
            -- tooltip read \`stage_label\` instead).
            m.dominant_stage as status_class,
            m.dominant_share,
            case
                when m.n_habitable_children = 0 then null
                else coalesce(a.acute_stage_share_raw, 0)
            end as acute_stage_share,
            m.n_habitable_children,
            m.is_dominant_fragile,
            m.has_incomplete_population,
            case m.dominant_stage
                when 'active-gentrification' then 'Active gentrification'
                when 'pioneer-signal' then 'Early pioneer signal'
                when 'improving-vulnerable' then 'Improving, vulnerable area'
                when 'pre-gentrification' then 'Pre-gentrification watch'
                when 'consolidation-pressure' then 'Consolidated, still intensifying'
                when 'stable-established' then 'Stable, established'
            end as stage_label,
            case m.dominant_stage
                when 'active-gentrification' then 1
                when 'pioneer-signal' then 2
                when 'improving-vulnerable' then 3
                when 'pre-gentrification' then 4
                when 'consolidation-pressure' then 5
                when 'stable-established' then 6
                else 99
            end as stage_sort,
            -- #310 review fix (MEDIUM-4): pairs the dominant-stage reading with an explicit
            -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment
            -- point 3) this must never be shown as a standalone, confident-looking label. Blank
            -- ('') rather than NULL when not fragile, so the tooltip line renders empty instead
            -- of a formatted "-" placeholder.
            case
                when m.is_dominant_fragile
                then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
                else ''
            end as fragile_note,
            -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit
            -- caveat whenever the weighting silently degraded to equal-weight for this area (see
            -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / MEDIUM-C fix: whenever this flag
            -- is true, the mart now equal-weights EVERY child in the area consistently -- never a
            -- silent partial-coverage mix of weighting schemes within the same area).
            case
                when m.has_incomplete_population
                then 'Population data incomplete for this area — equal-weighted, not population-weighted'
                else ''
            end as population_note,
            -- Rollup profile pages (all pre-existing routes): /berlin/area/<level>/<code>.
            '${ze}/berlin/area/${p.area_level.value}/' || m.area_code as link
        from gentriduck_marts.mart_area_rollup_stage_mix as m
        left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
        left join acute as a on m.area_code = a.area_code
        where m.area_level = '${p.area_level.value}'
          and m.city_code = 'BER'
          -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
          -- defends against a future second variant silently duplicating rows here.
          and m.variant = 'live_data'
          and m.period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
    )
select *
from base
order by stage_sort`;h.areas_rollup_data&&(h.areas_rollup_data instanceof Error?ne.initialError=h.areas_rollup_data:ne.initialData=h.areas_rollup_data,h.areas_rollup_columns&&(ne.knownColumns=h.areas_rollup_columns));let T,de=!1;const Ie=Fe.createReactive({callback:N=>{n(5,T=N)},execFn:y},{id:"areas_rollup",...ne});Ie($,{noResolve:ae,...ne}),globalThis[Symbol.for("areas_rollup")]={get value(){return T}};let pe={initialData:void 0,initialError:void 0},ce=fe`select
    area_name,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by dynamism_index desc`,_e=`select
    area_name,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by dynamism_index desc`;h.area_table_plr_data&&(h.area_table_plr_data instanceof Error?pe.initialError=h.area_table_plr_data:pe.initialData=h.area_table_plr_data,h.area_table_plr_columns&&(pe.knownColumns=h.area_table_plr_columns));let re,ie=!1;const ve=Fe.createReactive({callback:N=>{n(6,re=N)},execFn:y},{id:"area_table_plr",...pe});ve(_e,{noResolve:ce,...pe}),globalThis[Symbol.for("area_table_plr")]={get value(){return re}};let se={initialData:void 0,initialError:void 0},ye=fe`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (see that block's header comment; referenced by name in the actual \`left join\`
-- below) instead of duplicating the Bezirk code -> name CTE here -- not the raw (NULL-for-Bezirk)
-- mart column.
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (same construction as the \`areas_rollup\` query above --
-- see that block's header comment for the full rationale/citation) so the composition
-- counterweight is available in this table too, not just the map tooltip.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index desc\`. Ranking a citywide table by a population-weighted MEAN of an
-- ordinal class code is exactly the "presented, coloured, or ordered" construct the standing #267
-- domain decision forbids for a coarse scalar -- order by area_name instead (matches the
-- Stage-mix table below). status_index/dynamism_index remain visible as informational columns
-- (see the Column titles below, per the geo-DS's C1: "mean ordinal class (mean rank)", not a
-- score -- #310 review fix D2-R2: population-weighted where population data is complete and
-- equal-weighted otherwise, not unconditionally) -- keeping the mart's diagnostic columns
-- available is exactly what D1 remedy (a) intends; only the map-colour/rank uses are removed.
--
-- NB: see \`areas_rollup\`'s own header comment above for why \`acute_stage_share_raw\` is coalesced
-- to 0 below only when the area has at least one habitable child (the mart is sparse -- a filtered
-- SUM over zero matching rows is NULL, not 0).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
left join acute as a on m.area_code = a.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where
          area_level = '${p.area_level.value}' and city_code = 'BER'
          and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`,ke=`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (see that block's header comment; referenced by name in the actual \`left join\`
-- below) instead of duplicating the Bezirk code -> name CTE here -- not the raw (NULL-for-Bezirk)
-- mart column.
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (same construction as the \`areas_rollup\` query above --
-- see that block's header comment for the full rationale/citation) so the composition
-- counterweight is available in this table too, not just the map tooltip.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index desc\`. Ranking a citywide table by a population-weighted MEAN of an
-- ordinal class code is exactly the "presented, coloured, or ordered" construct the standing #267
-- domain decision forbids for a coarse scalar -- order by area_name instead (matches the
-- Stage-mix table below). status_index/dynamism_index remain visible as informational columns
-- (see the Column titles below, per the geo-DS's C1: "mean ordinal class (mean rank)", not a
-- score -- #310 review fix D2-R2: population-weighted where population data is complete and
-- equal-weighted otherwise, not unconditionally) -- keeping the mart's diagnostic columns
-- available is exactly what D1 remedy (a) intends; only the map-colour/rank uses are removed.
--
-- NB: see \`areas_rollup\`'s own header comment above for why \`acute_stage_share_raw\` is coalesced
-- to 0 below only when the area has at least one habitable child (the mart is sparse -- a filtered
-- SUM over zero matching rows is NULL, not 0).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
left join acute as a on m.area_code = a.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where
          area_level = '${p.area_level.value}' and city_code = 'BER'
          and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`;h.area_table_rollup_data&&(h.area_table_rollup_data instanceof Error?se.initialError=h.area_table_rollup_data:se.initialData=h.area_table_rollup_data,h.area_table_rollup_columns&&(se.knownColumns=h.area_table_rollup_columns));let me,Ue=!1;const Le=Fe.createReactive({callback:N=>{n(7,me=N)},execFn:y},{id:"area_table_rollup",...se});Le(ke,{noResolve:ye,...se}),globalThis[Symbol.for("area_table_rollup")]={get value(){return me}};let oe={initialData:void 0,initialError:void 0},xe=fe`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (referenced by name in the actual \`left join\` below), left-joined directly onto the
-- mart's full per-stage grain -- not this mart's raw (NULL-for-Bezirk) area_name column --
-- otherwise both this table's Bezirk rows AND its \`order by area_name\` degenerate to
-- blank/undefined for all 12 Bezirke.
--
-- #310 review fix (R-310-1): stage_label de-jargons m.typology_stage with the exact same
-- stage-name mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level and those can
-- have different has_incomplete_population values.
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    case m.typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else m.typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    m.stage_population_share,
    m.stage_n_children,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${p.area_level.value}' and city_code = 'BER' and variant = 'live_data'
  )
order by area_name, m.stage_population_share desc nulls last`,$e=`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (referenced by name in the actual \`left join\` below), left-joined directly onto the
-- mart's full per-stage grain -- not this mart's raw (NULL-for-Bezirk) area_name column --
-- otherwise both this table's Bezirk rows AND its \`order by area_name\` degenerate to
-- blank/undefined for all 12 Bezirke.
--
-- #310 review fix (R-310-1): stage_label de-jargons m.typology_stage with the exact same
-- stage-name mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level and those can
-- have different has_incomplete_population values.
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    case m.typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else m.typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    m.stage_population_share,
    m.stage_n_children,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${p.area_level.value}' and city_code = 'BER' and variant = 'live_data'
  )
order by area_name, m.stage_population_share desc nulls last`;h.area_mix_table_data&&(h.area_mix_table_data instanceof Error?oe.initialError=h.area_mix_table_data:oe.initialData=h.area_mix_table_data,h.area_mix_table_columns&&(oe.knownColumns=h.area_mix_table_columns));let ue,je=!1;const Me=Fe.createReactive({callback:N=>{n(8,ue=N)},execFn:y},{id:"area_mix_table",...oe});Me($e,{noResolve:xe,...oe}),globalThis[Symbol.for("area_mix_table")]={get value(){return ue}};const be={"Active gentrification":"#d73027","Early pioneer signal":"#fc8d59","Improving, vulnerable area":"#fee090","Pre-gentrification watch":"#e0f3f8","Consolidated, still intensifying":"#91bfdb","Stable, established":"#4575b4"};function Ze(N){const Ee=new Set,ge=[];for(const Ye of N??[]){const Ke=Ye==null?void 0:Ye.stage_label;Ke!=null&&!Ee.has(Ke)&&Ke in be&&(Ee.add(Ke),ge.push(be[Ke]))}return ge}const Oe={status_class:"Gentrification stage",status_index:"Social status",dynamism_index:"Dynamism"},Ae={plr:"plr_live_data.geojson",pgr:"pgr_lor2021.geojson",bezirk:"bezirk_lor2021.geojson",ortsteil:"ortsteil_self.geojson"},He={plr:"Planungsraum (PLR)",pgr:"Prognoseraum (PGR)",bezirk:"Bezirk",ortsteil:"Ortsteil"};return d.$$set=N=>{"data"in N&&n(14,b=N.data)},d.$$.update=()=>{d.$$.dirty[0]&16384&&n(15,{data:h={},customFormattingSettings:E,__db:g}=b,h),d.$$.dirty[0]&32768&&Ca.set(Object.keys(h).length>0),d.$$.dirty[1]&512&&w.params,d.$$.dirty[0]&983040&&(U||!he?U||(V(u,{noResolve:U,...A}),n(19,he=!0)):V(u,{noResolve:U})),d.$$.dirty[0]&15728640&&(Z||!J?Z||(te(j,{noResolve:Z,...ee}),n(23,J=!0)):te(j,{noResolve:Z})),d.$$.dirty[0]&1&&n(25,ae=fe`-- One row per rollup area (bezirk/pgr/ortsteil), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean, n_habitable_children,
-- is_dominant_fragile and has_incomplete_population are all CONSTANT across an area's stage rows
-- (see that mart's header), so any single row carries them; the row itself is not otherwise used
-- (the "Stage mix" table further down this page queries the full per-stage grain separately).
--
-- #310 review fix (MEDIUM-A): area_name falls back to the shared Bezirk-name lookup block above
-- (see that block's header comment; referenced by name in the actual \`left join\` below) wherever
-- the mart's own area_name is NULL -- every Bezirk row, since dim_area carries no bezirk-level
-- rows. At PGR/Ortsteil grain the mart's area_name is always populated, so the left join below is
-- a no-op there (no PGR/Ortsteil code ever matches a '01'..'12' Bezirk code).
--
-- NB: this comment deliberately avoids spelling out the lookup block's interpolation token in
-- prose (see the \`bezirk_names\` block's own header comment for why -- doing so would textually
-- substitute the lookup's entire compiled SQL into the middle of THIS single-line comment,
-- breaking out of the comment and corrupting the query, exactly as it would for a self-reference).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (Dangschat's invasion phase plus the Döring/Ulbricht
-- vulnerability case) from the mart's own per-(area, typology_stage) grain -- a plain aggregate
-- over already-published mix rows, no mart change, permitted under the standing #267 domain
-- decision's Recommendation 4 ("share of PLRs in active-gentrification typology" is an explicitly
-- allowed composition statistic). Filtering to one (city, level, period, variant) before grouping
-- by area_code cannot double-count, since that combination plus typology_stage is this mart's
-- natural key.
--
-- NB: the mart is SPARSE, not padded to all six stages per area (\`stage_agg\` there groups by
-- observed \`typology_stage\` only) -- so \`sum(...) filter (where typology_stage in (...))\` returns
-- NULL, not 0, for an area with real habitable children but none in an acute stage (zero matching
-- rows to sum). \`acute_stage_share_raw\` is therefore coalesced to 0 in \`base\` below, but ONLY when
-- the area has at least one habitable child -- an orphan area (n_habitable_children = 0) keeps a
-- genuine NULL, matching dominant_share's own convention.
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    ),
    base as (
        select
            m.city_code,
            m.area_code,
            coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
            m.status_index_weighted_mean as status_index,
            m.dynamism_index_weighted_mean as dynamism_index,
            -- dominant_stage as status_class kept for shape symmetry with \`areas_plr\`'s
            -- status_class column (not otherwise consumed downstream at this grain -- the map/
            -- tooltip read \`stage_label\` instead).
            m.dominant_stage as status_class,
            m.dominant_share,
            case
                when m.n_habitable_children = 0 then null
                else coalesce(a.acute_stage_share_raw, 0)
            end as acute_stage_share,
            m.n_habitable_children,
            m.is_dominant_fragile,
            m.has_incomplete_population,
            case m.dominant_stage
                when 'active-gentrification' then 'Active gentrification'
                when 'pioneer-signal' then 'Early pioneer signal'
                when 'improving-vulnerable' then 'Improving, vulnerable area'
                when 'pre-gentrification' then 'Pre-gentrification watch'
                when 'consolidation-pressure' then 'Consolidated, still intensifying'
                when 'stable-established' then 'Stable, established'
            end as stage_label,
            case m.dominant_stage
                when 'active-gentrification' then 1
                when 'pioneer-signal' then 2
                when 'improving-vulnerable' then 3
                when 'pre-gentrification' then 4
                when 'consolidation-pressure' then 5
                when 'stable-established' then 6
                else 99
            end as stage_sort,
            -- #310 review fix (MEDIUM-4): pairs the dominant-stage reading with an explicit
            -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment
            -- point 3) this must never be shown as a standalone, confident-looking label. Blank
            -- ('') rather than NULL when not fragile, so the tooltip line renders empty instead
            -- of a formatted "-" placeholder.
            case
                when m.is_dominant_fragile
                then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
                else ''
            end as fragile_note,
            -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit
            -- caveat whenever the weighting silently degraded to equal-weight for this area (see
            -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / MEDIUM-C fix: whenever this flag
            -- is true, the mart now equal-weights EVERY child in the area consistently -- never a
            -- silent partial-coverage mix of weighting schemes within the same area).
            case
                when m.has_incomplete_population
                then 'Population data incomplete for this area — equal-weighted, not population-weighted'
                else ''
            end as population_note,
            -- Rollup profile pages (all pre-existing routes): /berlin/area/<level>/<code>.
            '${ze}/berlin/area/${p.area_level.value}/' || m.area_code as link
        from gentriduck_marts.mart_area_rollup_stage_mix as m
        left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
        left join acute as a on m.area_code = a.area_code
        where m.area_level = '${p.area_level.value}'
          and m.city_code = 'BER'
          -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
          -- defends against a future second variant silently duplicating rows here.
          and m.variant = 'live_data'
          and m.period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
    )
select *
from base
order by stage_sort`),d.$$.dirty[0]&1&&n(26,$=`-- One row per rollup area (bezirk/pgr/ortsteil), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean, n_habitable_children,
-- is_dominant_fragile and has_incomplete_population are all CONSTANT across an area's stage rows
-- (see that mart's header), so any single row carries them; the row itself is not otherwise used
-- (the "Stage mix" table further down this page queries the full per-stage grain separately).
--
-- #310 review fix (MEDIUM-A): area_name falls back to the shared Bezirk-name lookup block above
-- (see that block's header comment; referenced by name in the actual \`left join\` below) wherever
-- the mart's own area_name is NULL -- every Bezirk row, since dim_area carries no bezirk-level
-- rows. At PGR/Ortsteil grain the mart's area_name is always populated, so the left join below is
-- a no-op there (no PGR/Ortsteil code ever matches a '01'..'12' Bezirk code).
--
-- NB: this comment deliberately avoids spelling out the lookup block's interpolation token in
-- prose (see the \`bezirk_names\` block's own header comment for why -- doing so would textually
-- substitute the lookup's entire compiled SQL into the middle of THIS single-line comment,
-- breaking out of the comment and corrupting the query, exactly as it would for a self-reference).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (Dangschat's invasion phase plus the Döring/Ulbricht
-- vulnerability case) from the mart's own per-(area, typology_stage) grain -- a plain aggregate
-- over already-published mix rows, no mart change, permitted under the standing #267 domain
-- decision's Recommendation 4 ("share of PLRs in active-gentrification typology" is an explicitly
-- allowed composition statistic). Filtering to one (city, level, period, variant) before grouping
-- by area_code cannot double-count, since that combination plus typology_stage is this mart's
-- natural key.
--
-- NB: the mart is SPARSE, not padded to all six stages per area (\`stage_agg\` there groups by
-- observed \`typology_stage\` only) -- so \`sum(...) filter (where typology_stage in (...))\` returns
-- NULL, not 0, for an area with real habitable children but none in an acute stage (zero matching
-- rows to sum). \`acute_stage_share_raw\` is therefore coalesced to 0 in \`base\` below, but ONLY when
-- the area has at least one habitable child -- an orphan area (n_habitable_children = 0) keeps a
-- genuine NULL, matching dominant_share's own convention.
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    ),
    base as (
        select
            m.city_code,
            m.area_code,
            coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
            m.status_index_weighted_mean as status_index,
            m.dynamism_index_weighted_mean as dynamism_index,
            -- dominant_stage as status_class kept for shape symmetry with \`areas_plr\`'s
            -- status_class column (not otherwise consumed downstream at this grain -- the map/
            -- tooltip read \`stage_label\` instead).
            m.dominant_stage as status_class,
            m.dominant_share,
            case
                when m.n_habitable_children = 0 then null
                else coalesce(a.acute_stage_share_raw, 0)
            end as acute_stage_share,
            m.n_habitable_children,
            m.is_dominant_fragile,
            m.has_incomplete_population,
            case m.dominant_stage
                when 'active-gentrification' then 'Active gentrification'
                when 'pioneer-signal' then 'Early pioneer signal'
                when 'improving-vulnerable' then 'Improving, vulnerable area'
                when 'pre-gentrification' then 'Pre-gentrification watch'
                when 'consolidation-pressure' then 'Consolidated, still intensifying'
                when 'stable-established' then 'Stable, established'
            end as stage_label,
            case m.dominant_stage
                when 'active-gentrification' then 1
                when 'pioneer-signal' then 2
                when 'improving-vulnerable' then 3
                when 'pre-gentrification' then 4
                when 'consolidation-pressure' then 5
                when 'stable-established' then 6
                else 99
            end as stage_sort,
            -- #310 review fix (MEDIUM-4): pairs the dominant-stage reading with an explicit
            -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment
            -- point 3) this must never be shown as a standalone, confident-looking label. Blank
            -- ('') rather than NULL when not fragile, so the tooltip line renders empty instead
            -- of a formatted "-" placeholder.
            case
                when m.is_dominant_fragile
                then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
                else ''
            end as fragile_note,
            -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit
            -- caveat whenever the weighting silently degraded to equal-weight for this area (see
            -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / MEDIUM-C fix: whenever this flag
            -- is true, the mart now equal-weights EVERY child in the area consistently -- never a
            -- silent partial-coverage mix of weighting schemes within the same area).
            case
                when m.has_incomplete_population
                then 'Population data incomplete for this area — equal-weighted, not population-weighted'
                else ''
            end as population_note,
            -- Rollup profile pages (all pre-existing routes): /berlin/area/<level>/<code>.
            '${ze}/berlin/area/${p.area_level.value}/' || m.area_code as link
        from gentriduck_marts.mart_area_rollup_stage_mix as m
        left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
        left join acute as a on m.area_code = a.area_code
        where m.area_level = '${p.area_level.value}'
          and m.city_code = 'BER'
          -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
          -- defends against a future second variant silently duplicating rows here.
          and m.variant = 'live_data'
          and m.period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
    )
select *
from base
order by stage_sort`),d.$$.dirty[0]&251658240&&(ae||!de?ae||(Ie($,{noResolve:ae,...ne}),n(27,de=!0)):Ie($,{noResolve:ae})),d.$$.dirty[0]&1879048192|d.$$.dirty[1]&1&&(ce||!ie?ce||(ve(_e,{noResolve:ce,...pe}),n(31,ie=!0)):ve(_e,{noResolve:ce})),d.$$.dirty[0]&1&&n(33,ye=fe`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (see that block's header comment; referenced by name in the actual \`left join\`
-- below) instead of duplicating the Bezirk code -> name CTE here -- not the raw (NULL-for-Bezirk)
-- mart column.
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (same construction as the \`areas_rollup\` query above --
-- see that block's header comment for the full rationale/citation) so the composition
-- counterweight is available in this table too, not just the map tooltip.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index desc\`. Ranking a citywide table by a population-weighted MEAN of an
-- ordinal class code is exactly the "presented, coloured, or ordered" construct the standing #267
-- domain decision forbids for a coarse scalar -- order by area_name instead (matches the
-- Stage-mix table below). status_index/dynamism_index remain visible as informational columns
-- (see the Column titles below, per the geo-DS's C1: "mean ordinal class (mean rank)", not a
-- score -- #310 review fix D2-R2: population-weighted where population data is complete and
-- equal-weighted otherwise, not unconditionally) -- keeping the mart's diagnostic columns
-- available is exactly what D1 remedy (a) intends; only the map-colour/rank uses are removed.
--
-- NB: see \`areas_rollup\`'s own header comment above for why \`acute_stage_share_raw\` is coalesced
-- to 0 below only when the area has at least one habitable child (the mart is sparse -- a filtered
-- SUM over zero matching rows is NULL, not 0).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
left join acute as a on m.area_code = a.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where
          area_level = '${p.area_level.value}' and city_code = 'BER'
          and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`),d.$$.dirty[0]&1&&n(34,ke=`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (see that block's header comment; referenced by name in the actual \`left join\`
-- below) instead of duplicating the Bezirk code -> name CTE here -- not the raw (NULL-for-Bezirk)
-- mart column.
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values (same construction as the \`areas_rollup\` query above --
-- see that block's header comment for the full rationale/citation) so the composition
-- counterweight is available in this table too, not just the map tooltip.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index desc\`. Ranking a citywide table by a population-weighted MEAN of an
-- ordinal class code is exactly the "presented, coloured, or ordered" construct the standing #267
-- domain decision forbids for a coarse scalar -- order by area_name instead (matches the
-- Stage-mix table below). status_index/dynamism_index remain visible as informational columns
-- (see the Column titles below, per the geo-DS's C1: "mean ordinal class (mean rank)", not a
-- score -- #310 review fix D2-R2: population-weighted where population data is complete and
-- equal-weighted otherwise, not unconditionally) -- keeping the mart's diagnostic columns
-- available is exactly what D1 remedy (a) intends; only the map-colour/rank uses are removed.
--
-- NB: see \`areas_rollup\`'s own header comment above for why \`acute_stage_share_raw\` is coalesced
-- to 0 below only when the area has at least one habitable child (the mart is sparse -- a filtered
-- SUM over zero matching rows is NULL, not 0).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${p.area_level.value}'
          and city_code = 'BER'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${p.area_level.value}' and city_code = 'BER'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
left join acute as a on m.area_code = a.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where
          area_level = '${p.area_level.value}' and city_code = 'BER'
          and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`),d.$$.dirty[1]&30&&(ye||!Ue?ye||(Le(ke,{noResolve:ye,...se}),n(35,Ue=!0)):Le(ke,{noResolve:ye})),d.$$.dirty[0]&1&&n(37,xe=fe`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (referenced by name in the actual \`left join\` below), left-joined directly onto the
-- mart's full per-stage grain -- not this mart's raw (NULL-for-Bezirk) area_name column --
-- otherwise both this table's Bezirk rows AND its \`order by area_name\` degenerate to
-- blank/undefined for all 12 Bezirke.
--
-- #310 review fix (R-310-1): stage_label de-jargons m.typology_stage with the exact same
-- stage-name mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level and those can
-- have different has_incomplete_population values.
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    case m.typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else m.typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    m.stage_population_share,
    m.stage_n_children,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${p.area_level.value}' and city_code = 'BER' and variant = 'live_data'
  )
order by area_name, m.stage_population_share desc nulls last`),d.$$.dirty[0]&1&&n(38,$e=`-- #310 review fix (MEDIUM-A): area_name fallback via the shared Bezirk-name lookup block defined
-- once above (referenced by name in the actual \`left join\` below), left-joined directly onto the
-- mart's full per-stage grain -- not this mart's raw (NULL-for-Bezirk) area_name column --
-- otherwise both this table's Bezirk rows AND its \`order by area_name\` degenerate to
-- blank/undefined for all 12 Bezirke.
--
-- #310 review fix (R-310-1): stage_label de-jargons m.typology_stage with the exact same
-- stage-name mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level and those can
-- have different has_incomplete_population values.
select
    coalesce(m.area_name, bn.bezirk_name, m.area_code) as area_name,
    case m.typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else m.typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    m.stage_population_share,
    m.stage_n_children,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join (-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)) as bn on m.area_code = bn.area_code
where m.area_level = '${p.area_level.value}'
  and m.city_code = 'BER'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${p.area_level.value}' and city_code = 'BER' and variant = 'live_data'
  )
order by area_name, m.stage_population_share desc nulls last`),d.$$.dirty[1]&480&&(xe||!je?xe||(Me($e,{noResolve:xe,...oe}),n(39,je=!0)):Me($e,{noResolve:xe})),d.$$.dirty[0]&1&&n(2,e=p.area_level.value!=="plr"),d.$$.dirty[0]&5&&n(1,r=e?"status_class":p.indicator.value),d.$$.dirty[0]&5&&n(11,i=e?"Gentrification stage — plain-language, colour-coded":p.indicator.label),d.$$.dirty[0]&1&&n(10,m=`${ze}/geo/${Ae[p.area_level.value]}`),d.$$.dirty[0]&6&&n(9,s=e?[{id:"area_name",showColumnName:!1,valueClass:"font-bold text-sm",fmt:"id"},{id:"stage_label",title:"Most widespread stage",fmt:"id"},{id:"dominant_share",title:"Most widespread stage's share",fmt:"pct0"},{id:"acute_stage_share",title:"Share in an acute-pressure stage",fmt:"pct0"},{id:"n_habitable_children",title:"Constituent areas with data",fmt:"id"},{id:"fragile_note",showColumnName:!1,valueClass:"text-xs text-amber-700",fmt:"id"},{id:"population_note",showColumnName:!1,valueClass:"text-xs text-amber-700",fmt:"id"},{id:"area_code",title:"Area code",valueClass:"text-xs opacity-60",fmt:"id"}]:[{id:"area_name",showColumnName:!1,valueClass:"font-bold text-sm",fmt:"id"},{id:r==="status_class"?"stage_label":r,title:Oe[r],fmt:r==="status_class"?"id":"num1"},{id:"area_code",title:"Area code",valueClass:"text-xs opacity-60",fmt:"id"}])},n(17,U=fe`-- #152: stage_label is the de-jargoned, human-readable form of status_class (typology_stage
-- from int_gentrification_ts's D1xD2 matrix, ADR-0008 -- no thresholds touched here, just a
-- friendlier string). stage_sort orders rows by gentrification-pressure severity (most acute
-- first) so this data's first-occurrence order of distinct stage_label values -- what
-- \`presentStagePalette()\` above (and EvidenceMap.js's own handleLegendValues/handleFillColor)
-- keys its colour lookup on -- is deterministic and matches the "worse -> red" reading order,
-- regardless of DuckDB's natural row order. Ordering rationale (Dangschat 1988 double
-- invasion-succession cycle; Döring & Ulbricht 2016 vulnerability framework -- both cited in
-- int_gentrification_ts.sql):
--   1 active-gentrification  -- mid-status area improving fastest: gentrification in motion.
--   2 pioneer-signal         -- low-status area improving fast: earliest displacement signal.
--   3 improving-vulnerable   -- most-deprived area improving: vulnerable population, watch.
--   4 pre-gentrification     -- early/mixed signals (filtering-down or nascent upgrading).
--   5 consolidation-pressure -- already-affluent area still intensifying: lower urgency.
--   6 stable-established     -- no material status change: least pressure.
select
    city_code,
    area_code,
    area_name,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- Drill-down click-through target (#133 G1d, exact-code fix #150): /berlin/area/[code]
    -- queries fct_gentrification_change etc. on lor_2021 (current, 542-PLR) area codes, which
    -- this \`live_data\`/\`plr\` query always returns -- see #309 for why the \`standard\` variant
    -- (pre-2021 codes, no click-through) no longer appears on this page at all.
    '${ze}/berlin/area/' || area_code as link
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by stage_sort`),n(18,u=`-- #152: stage_label is the de-jargoned, human-readable form of status_class (typology_stage
-- from int_gentrification_ts's D1xD2 matrix, ADR-0008 -- no thresholds touched here, just a
-- friendlier string). stage_sort orders rows by gentrification-pressure severity (most acute
-- first) so this data's first-occurrence order of distinct stage_label values -- what
-- \`presentStagePalette()\` above (and EvidenceMap.js's own handleLegendValues/handleFillColor)
-- keys its colour lookup on -- is deterministic and matches the "worse -> red" reading order,
-- regardless of DuckDB's natural row order. Ordering rationale (Dangschat 1988 double
-- invasion-succession cycle; Döring & Ulbricht 2016 vulnerability framework -- both cited in
-- int_gentrification_ts.sql):
--   1 active-gentrification  -- mid-status area improving fastest: gentrification in motion.
--   2 pioneer-signal         -- low-status area improving fast: earliest displacement signal.
--   3 improving-vulnerable   -- most-deprived area improving: vulnerable population, watch.
--   4 pre-gentrification     -- early/mixed signals (filtering-down or nascent upgrading).
--   5 consolidation-pressure -- already-affluent area still intensifying: lower urgency.
--   6 stable-established     -- no material status change: least pressure.
select
    city_code,
    area_code,
    area_name,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- Drill-down click-through target (#133 G1d, exact-code fix #150): /berlin/area/[code]
    -- queries fct_gentrification_change etc. on lor_2021 (current, 542-PLR) area codes, which
    -- this \`live_data\`/\`plr\` query always returns -- see #309 for why the \`standard\` variant
    -- (pre-2021 codes, no click-through) no longer appears on this page at all.
    '${ze}/berlin/area/' || area_code as link
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by stage_sort`),n(21,Z=fe`-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)`),n(22,j=`-- #310 review fix (MEDIUM-A): fixed 12-entry Berlin Bezirk code -> name lookup, needed because
-- mart_area_rollup_stage_mix.area_name is NULL for every Berlin Bezirk row (that mart's own
-- documented gap -- dim_area carries no bezirk-level rows, see this mart's header). Kept in sync
-- with web/scripts/export_area_geojson.py's BEZIRK_NAMES constant if Bezirk names/boundaries ever
-- change.
--
-- Defined ONCE here, as its own top-level SQL block, and referenced BY NAME using Evidence's own
-- block-interpolation syntax from the \`areas_rollup\`, \`area_table_rollup\`, and \`area_mix_table\`
-- blocks below, instead of repeating this lookup three times (see those blocks for the actual
-- reference). An earlier revision of this fix claimed Evidence/DuckDB-WASM does not support one
-- page block referencing another block's result, and duplicated this lookup verbatim into three
-- separate blocks instead -- that claim was wrong. Per
-- @evidence-dev/preprocess/src/extract-queries/extract-queries.cjs's query-chaining step, that
-- interpolation syntax textually inlines the referenced block as a parenthesised subquery
-- wherever it appears in another block's text (confirmed against the installed Evidence version
-- with a real \`evidence build\`). The "Catalog Error: Table with name ... does not exist" the
-- original author hit came from referencing a block by its BARE name as a table identifier (e.g.
-- \`from base\`), not from this interpolation syntax.
--
-- NB: this comment deliberately avoids spelling out this block's own reference token literally --
-- Evidence's query-chaining resolver scans EVERY block's full text (comments included) for that
-- exact token, so writing this block's own name inside that syntax, in its own text, creates a
-- literal self-reference that the resolver reports as "Compiler error: circular reference" (hit
-- and fixed during this review pass -- see the actual reference in \`areas_rollup\` etc. below for
-- what this looks like when it is NOT self-referential).
select
    area_code,
    case area_code
        when '01' then 'Mitte'
        when '02' then 'Friedrichshain-Kreuzberg'
        when '03' then 'Pankow'
        when '04' then 'Charlottenburg-Wilmersdorf'
        when '05' then 'Spandau'
        when '06' then 'Steglitz-Zehlendorf'
        when '07' then 'Tempelhof-Schöneberg'
        when '08' then 'Neukölln'
        when '09' then 'Treptow-Köpenick'
        when '10' then 'Marzahn-Hellersdorf'
        when '11' then 'Lichtenberg'
        when '12' then 'Reinickendorf'
    end as bezirk_name
from
    (
        values
            ('01'),
            ('02'),
            ('03'),
            ('04'),
            ('05'),
            ('06'),
            ('07'),
            ('08'),
            ('09'),
            ('10'),
            ('11'),
            ('12')
    ) as t (area_code)`),n(29,ce=fe`select
    area_name,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by dynamism_index desc`),n(30,_e=`select
    area_name,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data'
  and area_level = 'plr'
  and city_code = 'BER'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'plr'
  )
order by dynamism_index desc`),[p,r,e,q,we,T,re,me,ue,s,m,i,Ze,He,b,h,A,U,u,he,ee,Z,j,J,ne,ae,$,de,pe,ce,_e,ie,se,ye,ke,Ue,oe,xe,$e,je,w]}class ft extends Ea{constructor(a){super(),Ba(this,a,tt,at,wa,{data:14},null,[-1,-1])}}export{ft as component};
