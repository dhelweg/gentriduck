import{s as Xe,d as o,i as d,a as Ie,b as T,c as $,h as Ze,e as I,f as Le,g as R,j as k,k as L,l as Pe,m as ea,o as aa,n as ta,p as ia,q as X,t as O,u as F,r as na}from"../chunks/scheduler.BopPEjhc.js";import{S as ra,i as sa,d as S,t as v,a as w,c as xe,m as D,b as E,e as A,g as $e}from"../chunks/index.CYkVJg6_.js";import{F as la}from"../chunks/FooterNav.Q7MmbWm3.js";import{H as oa}from"../chunks/Hero.CRoRGI02.js";import{D as Ge,C as V}from"../chunks/VennDiagram.svelte_svelte_type_style_lang.DrVUZ76A.js";import{b as da,w as ma}from"../chunks/entry.BMmpG6A7.js";import{A as Me}from"../chunks/Alert.BO8kFSQK.js";import{e as _a,s as ca,Q as Ce,p as ua,a as je,r as Oe,C as ha}from"../chunks/inferColumnTypes.B1nNps41.js";import{h as me}from"../chunks/setTrackProxy.Cyfckp0w.js";import{D as Ke,a as He}from"../chunks/Dropdown.BxlIFH-r.js";import{p as pa}from"../chunks/stores.Ceyp10jj.js";import{Q as Se}from"../chunks/QueryViewer.CNDqAaoE.js";import{A as ga}from"../chunks/AreaMap.w9kcpT6D.js";import{p as fa}from"../chunks/profile.BW8tN6E9.js";function ba(_){let a,r=G.title+"",e;return{c(){a=L("h1"),e=F(r),this.h()},l(n){a=I(n,"H1",{class:!0});var s=na(a);e=O(s,r),s.forEach(o),this.h()},h(){T(a,"class","title")},m(n,s){d(n,a,s),Ie(a,e)},p:X,d(n){n&&o(a)}}}function wa(_){return{c(){this.h()},l(a){this.h()},h(){document.title="Evidence"},m:X,p:X,d:X}}function ya(_){let a,r,e,n,s;return document.title=a=G.title,{c(){r=k(),e=L("meta"),n=k(),s=L("meta"),this.h()},l(m){r=$(m),e=I(m,"META",{property:!0,content:!0}),n=$(m),s=I(m,"META",{name:!0,content:!0}),this.h()},h(){var m,i;T(e,"property","og:title"),T(e,"content",((m=G.og)==null?void 0:m.title)??G.title),T(s,"name","twitter:title"),T(s,"content",((i=G.og)==null?void 0:i.title)??G.title)},m(m,i){d(m,r,i),d(m,e,i),d(m,n,i),d(m,s,i)},p(m,i){i&0&&a!==(a=G.title)&&(document.title=a)},d(m){m&&(o(r),o(e),o(n),o(s))}}}function va(_){var s,m;let a,r,e=(G.description||((s=G.og)==null?void 0:s.description))&&xa(),n=((m=G.og)==null?void 0:m.image)&&$a();return{c(){e&&e.c(),a=k(),n&&n.c(),r=Le()},l(i){e&&e.l(i),a=$(i),n&&n.l(i),r=Le()},m(i,f){e&&e.m(i,f),d(i,a,f),n&&n.m(i,f),d(i,r,f)},p(i,f){var p,b;(G.description||(p=G.og)!=null&&p.description)&&e.p(i,f),(b=G.og)!=null&&b.image&&n.p(i,f)},d(i){i&&(o(a),o(r)),e&&e.d(i),n&&n.d(i)}}}function xa(_){let a,r,e,n,s;return{c(){a=L("meta"),r=k(),e=L("meta"),n=k(),s=L("meta"),this.h()},l(m){a=I(m,"META",{name:!0,content:!0}),r=$(m),e=I(m,"META",{property:!0,content:!0}),n=$(m),s=I(m,"META",{name:!0,content:!0}),this.h()},h(){var m,i,f;T(a,"name","description"),T(a,"content",G.description??((m=G.og)==null?void 0:m.description)),T(e,"property","og:description"),T(e,"content",((i=G.og)==null?void 0:i.description)??G.description),T(s,"name","twitter:description"),T(s,"content",((f=G.og)==null?void 0:f.description)??G.description)},m(m,i){d(m,a,i),d(m,r,i),d(m,e,i),d(m,n,i),d(m,s,i)},p:X,d(m){m&&(o(a),o(r),o(e),o(n),o(s))}}}function $a(_){let a,r,e;return{c(){a=L("meta"),r=k(),e=L("meta"),this.h()},l(n){a=I(n,"META",{property:!0,content:!0}),r=$(n),e=I(n,"META",{name:!0,content:!0}),this.h()},h(){var n,s;T(a,"property","og:image"),T(a,"content",je((n=G.og)==null?void 0:n.image)),T(e,"name","twitter:image"),T(e,"content",je((s=G.og)==null?void 0:s.image))},m(n,s){d(n,a,s),d(n,r,s),d(n,e,s)},p:X,d(n){n&&(o(a),o(r),o(e))}}}function ka(_){let a,r="How to read the map:",e,n,s='"Gentrification stage"',m,i,f="Berlin's map",p,b,c='"Social status"',y,h,u='"Dynamism"',M,q,N=`more
  deprived`,U,l,H="faster",ie,W,ne="3-year",K,Q,re="methodology §6",Y;return{c(){a=L("b"),a.textContent=r,e=F(" the "),n=L("b"),n.textContent=s,m=F(` option colours each area by one of
  six plain-language stages (red = highest pressure / earliest displacement risk, blue = most
  stable) — the same six-stage typology used on `),i=L("a"),i.textContent=f,p=F(`. At the
  statistisches Gebiet level, the `),b=L("b"),b.textContent=c,y=F(" and "),h=L("b"),h.textContent=u,M=F(` options are also
  offered, showing the raw ordinal inputs (not offered as a map colour at Stadtteil/Bezirk grain —
  see the Indicator note there for why): "Social status" is ordinal — higher shading means `),q=L("b"),q.textContent=N,U=F(`, not more prosperous. "Dynamism" — higher means the area's status is improving
  `),l=L("b"),l.textContent=H,ie=F(", over Hamburg's own "),W=L("b"),W.textContent=ne,K=F(` observation window (Berlin's is 2 years) — see
  `),Q=L("a"),Q.textContent=re,Y=F(` for why that is a qualitative difference in what
  "active" means, not just a numeric-scale one. Areas without a value are drawn but left blank.`),this.h()},l(x){a=I(x,"B",{"data-svelte-h":!0}),R(a)!=="svelte-1k0da8y"&&(a.textContent=r),e=O(x," the "),n=I(x,"B",{"data-svelte-h":!0}),R(n)!=="svelte-1sxo7gq"&&(n.textContent=s),m=O(x,` option colours each area by one of
  six plain-language stages (red = highest pressure / earliest displacement risk, blue = most
  stable) — the same six-stage typology used on `),i=I(x,"A",{href:!0,"data-svelte-h":!0}),R(i)!=="svelte-1xjrx70"&&(i.textContent=f),p=O(x,`. At the
  statistisches Gebiet level, the `),b=I(x,"B",{"data-svelte-h":!0}),R(b)!=="svelte-1tb9m99"&&(b.textContent=c),y=O(x," and "),h=I(x,"B",{"data-svelte-h":!0}),R(h)!=="svelte-108pztc"&&(h.textContent=u),M=O(x,` options are also
  offered, showing the raw ordinal inputs (not offered as a map colour at Stadtteil/Bezirk grain —
  see the Indicator note there for why): "Social status" is ordinal — higher shading means `),q=I(x,"B",{"data-svelte-h":!0}),R(q)!=="svelte-q56ery"&&(q.textContent=N),U=O(x,`, not more prosperous. "Dynamism" — higher means the area's status is improving
  `),l=I(x,"B",{"data-svelte-h":!0}),R(l)!=="svelte-1ij4xx3"&&(l.textContent=H),ie=O(x,", over Hamburg's own "),W=I(x,"B",{"data-svelte-h":!0}),R(W)!=="svelte-cks6f7"&&(W.textContent=ne),K=O(x,` observation window (Berlin's is 2 years) — see
  `),Q=I(x,"A",{href:!0,"data-svelte-h":!0}),R(Q)!=="svelte-fe16dh"&&(Q.textContent=re),Y=O(x,` for why that is a qualitative difference in what
  "active" means, not just a numeric-scale one. Areas without a value are drawn but left blank.`),this.h()},h(){T(i,"href","/gentriduck/berlin/maps"),T(Q,"href","/gentriduck/methodology")},m(x,B){d(x,a,B),d(x,e,B),d(x,n,B),d(x,m,B),d(x,i,B),d(x,p,B),d(x,b,B),d(x,y,B),d(x,h,B),d(x,M,B),d(x,q,B),d(x,U,B),d(x,l,B),d(x,ie,B),d(x,W,B),d(x,K,B),d(x,Q,B),d(x,Y,B)},p:X,d(x){x&&(o(a),o(e),o(n),o(m),o(i),o(p),o(b),o(y),o(h),o(M),o(q),o(U),o(l),o(ie),o(W),o(K),o(Q),o(Y))}}}function Ha(_){let a,r="This map has no neighbourhood names at the Gebiet level.",e,n,s="the Hamburg data hub",m,i,f="This map is never shown alongside, or blended with, Berlin's own map",p,b,c="methodology §6",y;return{c(){a=L("b"),a.textContent=r,e=F(` Hamburg's statistische Gebiete
  carry only a numeric code in the source geodata (unlike Berlin's named Planungsräume or
  Hamburg's own Stadtteile/Bezirke) — hover any Gebiet for its code and value. There is no
  per-area drill-down page for Hamburg's Gebiet level yet (Berlin's equivalent needs data — a
  trajectory history, price/rent — that does not yet exist for Hamburg; see
  `),n=L("a"),n.textContent=s,m=F(` for the full inventory of what is and isn't built).
  `),i=L("b"),i.textContent=f,p=F(` — Hamburg and Berlin
  each have their own route, their own colour-scale computation, and are never offered as a
  side-by-side or subtracted comparison (see `),b=L("a"),b.textContent=c,y=F(")."),this.h()},l(h){a=I(h,"B",{"data-svelte-h":!0}),R(a)!=="svelte-hzo4au"&&(a.textContent=r),e=O(h,` Hamburg's statistische Gebiete
  carry only a numeric code in the source geodata (unlike Berlin's named Planungsräume or
  Hamburg's own Stadtteile/Bezirke) — hover any Gebiet for its code and value. There is no
  per-area drill-down page for Hamburg's Gebiet level yet (Berlin's equivalent needs data — a
  trajectory history, price/rent — that does not yet exist for Hamburg; see
  `),n=I(h,"A",{href:!0,"data-svelte-h":!0}),R(n)!=="svelte-rsbj88"&&(n.textContent=s),m=O(h,` for the full inventory of what is and isn't built).
  `),i=I(h,"B",{"data-svelte-h":!0}),R(i)!=="svelte-1dputrs"&&(i.textContent=f),p=O(h,` — Hamburg and Berlin
  each have their own route, their own colour-scale computation, and are never offered as a
  side-by-side or subtracted comparison (see `),b=I(h,"A",{href:!0,"data-svelte-h":!0}),R(b)!=="svelte-fe16dh"&&(b.textContent=c),y=O(h,")."),this.h()},h(){T(n,"href","/gentriduck/hamburg"),T(b,"href","/gentriduck/methodology")},m(h,u){d(h,a,u),d(h,e,u),d(h,n,u),d(h,m,u),d(h,i,u),d(h,p,u),d(h,b,u),d(h,y,u)},p:X,d(h){h&&(o(a),o(e),o(n),o(m),o(i),o(p),o(b),o(y))}}}function qa(_){let a,r,e,n,s,m;return a=new He({props:{value:"subarea_l2",valueLabel:"Statistisches Gebiet — finest detail (no names)"}}),e=new He({props:{value:"subarea_l1",valueLabel:"Stadtteil — ~104 named districts"}}),s=new He({props:{value:"district",valueLabel:"Bezirk — 7 boroughs"}}),{c(){A(a.$$.fragment),r=k(),A(e.$$.fragment),n=k(),A(s.$$.fragment)},l(i){E(a.$$.fragment,i),r=$(i),E(e.$$.fragment,i),n=$(i),E(s.$$.fragment,i)},m(i,f){D(a,i,f),d(i,r,f),D(e,i,f),d(i,n,f),D(s,i,f),m=!0},p:X,i(i){m||(w(a.$$.fragment,i),w(e.$$.fragment,i),w(s.$$.fragment,i),m=!0)},o(i){v(a.$$.fragment,i),v(e.$$.fragment,i),v(s.$$.fragment,i),m=!1},d(i){i&&(o(r),o(n)),S(a,i),S(e,i),S(s,i)}}}function Fe(_){let a,r,e,n;return a=new Me({props:{status:"info",$$slots:{default:[Ca]},$$scope:{ctx:_}}}),e=new Me({props:{status:"warning",$$slots:{default:[Sa]},$$scope:{ctx:_}}}),{c(){A(a.$$.fragment),r=k(),A(e.$$.fragment)},l(s){E(a.$$.fragment,s),r=$(s),E(e.$$.fragment,s)},m(s,m){D(a,s,m),d(s,r,m),D(e,s,m),n=!0},i(s){n||(w(a.$$.fragment,s),w(e.$$.fragment,s),n=!0)},o(s){v(a.$$.fragment,s),v(e.$$.fragment,s),n=!1},d(s){s&&o(r),S(a,s),S(e,s)}}}function Ca(_){let a,r=`Stadtteil / Bezirk are population-weighted rollups, where possible, of the Gebiet-level
  data`,e,n,s="#310 design decision",m,i,f='most widespread ("dominant") stage',p,b,c="share",y,h,u="Hamburg's latest published period has no population figures for effectively any Gebiet",M,q,N="equal",U;return{c(){a=L("b"),a.textContent=r,e=F(` — never a re-scored index at this grain (averaging the underlying MSS-equivalent
  ordinals into a single new category would be statistically invalid; see the
  `),n=L("a"),n.textContent=s,m=F(`, the same
  decision applied unmodified to Berlin's Bezirk/PGR/Ortsteil rollups). The map colours by the
  `),i=L("b"),i.textContent=f,p=F(` among that area's constituent Gebiete, weighted by
  population where available — its tooltip and the tables below always show that stage's
  `),b=L("b"),b.textContent=c,y=F(` alongside it (population-weighted where possible, equal-weighted as a flagged
  fallback otherwise), and the "Stage mix" table further down shows the full breakdown. Areas with
  fewer than 3 Gebiete with real data are flagged as a fragile ("small sample") reading.
  `),h=L("b"),h.textContent=u,M=F(` —
  every Stadtteil/Bezirk rollup shown here currently falls back to `),q=L("b"),q.textContent=N,U=F(` weighting across
  its constituent Gebiete rather than true population weighting, flagged as "population data
  incomplete" in the table and map tooltip below.`),this.h()},l(l){a=I(l,"B",{"data-svelte-h":!0}),R(a)!=="svelte-1gbvnu3"&&(a.textContent=r),e=O(l,` — never a re-scored index at this grain (averaging the underlying MSS-equivalent
  ordinals into a single new category would be statistically invalid; see the
  `),n=I(l,"A",{href:!0,"data-svelte-h":!0}),R(n)!=="svelte-vym2cu"&&(n.textContent=s),m=O(l,`, the same
  decision applied unmodified to Berlin's Bezirk/PGR/Ortsteil rollups). The map colours by the
  `),i=I(l,"B",{"data-svelte-h":!0}),R(i)!=="svelte-1d2azc6"&&(i.textContent=f),p=O(l,` among that area's constituent Gebiete, weighted by
  population where available — its tooltip and the tables below always show that stage's
  `),b=I(l,"B",{"data-svelte-h":!0}),R(b)!=="svelte-dsjmq7"&&(b.textContent=c),y=O(l,` alongside it (population-weighted where possible, equal-weighted as a flagged
  fallback otherwise), and the "Stage mix" table further down shows the full breakdown. Areas with
  fewer than 3 Gebiete with real data are flagged as a fragile ("small sample") reading.
  `),h=I(l,"B",{"data-svelte-h":!0}),R(h)!=="svelte-w70dn4"&&(h.textContent=u),M=O(l,` —
  every Stadtteil/Bezirk rollup shown here currently falls back to `),q=I(l,"B",{"data-svelte-h":!0}),R(q)!=="svelte-12wzsvk"&&(q.textContent=N),U=O(l,` weighting across
  its constituent Gebiete rather than true population weighting, flagged as "population data
  incomplete" in the table and map tooltip below.`),this.h()},h(){T(n,"href","https://github.com/dhelweg/gentriduck/issues/310")},m(l,H){d(l,a,H),d(l,e,H),d(l,n,H),d(l,m,H),d(l,i,H),d(l,p,H),d(l,b,H),d(l,y,H),d(l,h,H),d(l,M,H),d(l,q,H),d(l,U,H)},p:X,d(l){l&&(o(a),o(e),o(n),o(m),o(i),o(p),o(b),o(y),o(h),o(M),o(q),o(U))}}}function Sa(_){let a,r='A "Stable, established" reading at this grain is not evidence that pressure is absent.',e,n,s=`least
  acute`,m;return{c(){a=L("b"),a.textContent=r,e=F(`
  Plurality voting resolves an area to whichever stage is most widespread among its constituent
  Gebiete — and because "Stable, established" is, by construction, the most common and `),n=L("b"),n.textContent=s,m=F(` stage citywide, coarser levels are systematically biased toward reading as calm, not
  toward neutral noise. An area whose map colour reads "Stable, established" can still contain
  neighbourhoods under active gentrification pressure; this map alone cannot show you whether it
  does. The share of the area in an acute-pressure stage (residents where population data exists,
  otherwise constituent areas — in the tooltip and table below) and the statistisches Gebiet
  level — the finest grain this site publishes — are where that pressure is actually locatable.`)},l(i){a=I(i,"B",{"data-svelte-h":!0}),R(a)!=="svelte-uzid1v"&&(a.textContent=r),e=O(i,`
  Plurality voting resolves an area to whichever stage is most widespread among its constituent
  Gebiete — and because "Stable, established" is, by construction, the most common and `),n=I(i,"B",{"data-svelte-h":!0}),R(n)!=="svelte-r2ff7v"&&(n.textContent=s),m=O(i,` stage citywide, coarser levels are systematically biased toward reading as calm, not
  toward neutral noise. An area whose map colour reads "Stable, established" can still contain
  neighbourhoods under active gentrification pressure; this map alone cannot show you whether it
  does. The share of the area in an acute-pressure stage (residents where population data exists,
  otherwise constituent areas — in the tooltip and table below) and the statistisches Gebiet
  level — the finest grain this site publishes — are where that pressure is actually locatable.`)},m(i,f){d(i,a,f),d(i,e,f),d(i,n,f),d(i,m,f)},p:X,d(i){i&&(o(a),o(e),o(n),o(m))}}}function Da(_){let a,r=`<b>Indicator: Gentrification stage</b> — the only map colour offered at this area level. A
  population-weighted <i>mean</i> of the Social status / Dynamism ordinals is not offered as a
  coloured, ranked map indicator at Stadtteil/Bezirk grain (averaging discrete ordinal classes into
  a coarse-grain score and colouring or ranking by it is a documented methodology red line for
  this project — see the <a href="/gentriduck/methodology">methodology &amp; data sources</a> page); those raw
  ordinals remain available, clearly labelled as a mean ordinal class (mean rank),
  population-weighted where population data is complete and equal-weighted otherwise, in the
  table below and as a real map colour at the statistisches Gebiet level above.`;return{c(){a=L("p"),a.innerHTML=r,this.h()},l(e){a=I(e,"P",{class:!0,"data-svelte-h":!0}),R(a)!=="svelte-re7ig2"&&(a.innerHTML=r),this.h()},h(){T(a,"class","text-sm mb-2")},m(e,n){d(e,a,n)},i:X,o:X,d(e){e&&o(a)}}}function Ea(_){let a,r;return a=new Ke({props:{name:"indicator",title:"Indicator",defaultValue:"status_class",$$slots:{default:[Aa]},$$scope:{ctx:_}}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function Aa(_){let a,r,e,n,s,m;return a=new He({props:{value:"status_class",valueLabel:"Gentrification stage — plain-language, colour-coded"}}),e=new He({props:{value:"status_index",valueLabel:"Social status — how deprived or affluent (current snapshot)"}}),s=new He({props:{value:"dynamism_index",valueLabel:"Dynamism — how fast that status is changing (3-year window)"}}),{c(){A(a.$$.fragment),r=k(),A(e.$$.fragment),n=k(),A(s.$$.fragment)},l(i){E(a.$$.fragment,i),r=$(i),E(e.$$.fragment,i),n=$(i),E(s.$$.fragment,i)},m(i,f){D(a,i,f),d(i,r,f),D(e,i,f),d(i,n,f),D(s,i,f),m=!0},p:X,i(i){m||(w(a.$$.fragment,i),w(e.$$.fragment,i),w(s.$$.fragment,i),m=!0)},o(i){v(a.$$.fragment,i),v(e.$$.fragment,i),v(s.$$.fragment,i),m=!1},d(i){i&&(o(r),o(n)),S(a,i),S(e,i),S(s,i)}}}function We(_){let a,r;return a=new Se({props:{queryID:"areas_leaf",queryResult:_[3]}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},p(e,n){const s={};n[0]&8&&(s.queryResult=e[3]),a.$set(s)},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function Qe(_){let a,r;return a=new Se({props:{queryID:"areas_rollup",queryResult:_[4]}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},p(e,n){const s={};n[0]&16&&(s.queryResult=e[4]),a.$set(s)},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function Ye(_){let a,r;return a=new Se({props:{queryID:"area_table_leaf",queryResult:_[5]}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},p(e,n){const s={};n[0]&32&&(s.queryResult=e[5]),a.$set(s)},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function Je(_){let a,r;return a=new Se({props:{queryID:"area_table_rollup",queryResult:_[6]}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},p(e,n){const s={};n[0]&64&&(s.queryResult=e[6]),a.$set(s)},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function Ia(_){let a,r;return a=new Ge({props:{data:_[5],rows:"10",$$slots:{default:[Ma]},$$scope:{ctx:_}}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},p(e,n){const s={};n[0]&32&&(s.data=e[5]),n[1]&2097152&&(s.$$scope={dirty:n,ctx:e}),a.$set(s)},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function La(_){let a,r,e,n='<a href="#stage-mix--full-breakdown-per-area">Stage mix — full breakdown per area</a>',s,m,i=`Never rely on the most widespread stage alone: this table is the full stage-mix distribution
behind every area above — population-weighted where an area&#39;s population data is complete,
equal-weighted as a flagged fallback otherwise (see the &quot;population data incomplete&quot; column) —
including the <code class="markdown">uninhabited / no data</code> bucket, shown as a count of constituent areas, not a share
(it is excluded from the share calculation by design; see that column&#39;s own values below).`,f,p,b,c;a=new Ge({props:{data:_[6],rows:"10",$$slots:{default:[Ta]},$$scope:{ctx:_}}});let y=_[7]&&Ve(_);return b=new Ge({props:{data:_[7],rows:"50",search:"true",$$slots:{default:[Ba]},$$scope:{ctx:_}}}),{c(){A(a.$$.fragment),r=k(),e=L("h3"),e.innerHTML=n,s=k(),m=L("p"),m.innerHTML=i,f=k(),y&&y.c(),p=k(),A(b.$$.fragment),this.h()},l(h){E(a.$$.fragment,h),r=$(h),e=I(h,"H3",{class:!0,id:!0,"data-svelte-h":!0}),R(e)!=="svelte-gqegou"&&(e.innerHTML=n),s=$(h),m=I(h,"P",{class:!0,"data-svelte-h":!0}),R(m)!=="svelte-114mt3e"&&(m.innerHTML=i),f=$(h),y&&y.l(h),p=$(h),E(b.$$.fragment,h),this.h()},h(){T(e,"class","markdown"),T(e,"id","stage-mix--full-breakdown-per-area"),T(m,"class","markdown")},m(h,u){D(a,h,u),d(h,r,u),d(h,e,u),d(h,s,u),d(h,m,u),d(h,f,u),y&&y.m(h,u),d(h,p,u),D(b,h,u),c=!0},p(h,u){const M={};u[0]&64&&(M.data=h[6]),u[1]&2097152&&(M.$$scope={dirty:u,ctx:h}),a.$set(M),h[7]?y?(y.p(h,u),u[0]&128&&w(y,1)):(y=Ve(h),y.c(),w(y,1),y.m(p.parentNode,p)):y&&($e(),v(y,1,1,()=>{y=null}),xe());const q={};u[0]&128&&(q.data=h[7]),u[1]&2097152&&(q.$$scope={dirty:u,ctx:h}),b.$set(q)},i(h){c||(w(a.$$.fragment,h),w(y),w(b.$$.fragment,h),c=!0)},o(h){v(a.$$.fragment,h),v(y),v(b.$$.fragment,h),c=!1},d(h){h&&(o(r),o(e),o(s),o(m),o(f),o(p)),S(a,h),y&&y.d(h),S(b,h)}}}function Ma(_){let a,r,e,n,s,m,i,f;return a=new V({props:{id:"area_code",title:"Gebiet code"}}),e=new V({props:{id:"status_index",title:"Social status (1=least deprived … 4=most deprived)"}}),s=new V({props:{id:"dynamism_index",title:"Speed of change (higher = faster upward change, 3-year window)"}}),i=new V({props:{id:"stage_label",title:"Gentrification stage"}}),{c(){A(a.$$.fragment),r=k(),A(e.$$.fragment),n=k(),A(s.$$.fragment),m=k(),A(i.$$.fragment)},l(p){E(a.$$.fragment,p),r=$(p),E(e.$$.fragment,p),n=$(p),E(s.$$.fragment,p),m=$(p),E(i.$$.fragment,p)},m(p,b){D(a,p,b),d(p,r,b),D(e,p,b),d(p,n,b),D(s,p,b),d(p,m,b),D(i,p,b),f=!0},p:X,i(p){f||(w(a.$$.fragment,p),w(e.$$.fragment,p),w(s.$$.fragment,p),w(i.$$.fragment,p),f=!0)},o(p){v(a.$$.fragment,p),v(e.$$.fragment,p),v(s.$$.fragment,p),v(i.$$.fragment,p),f=!1},d(p){p&&(o(r),o(n),o(m)),S(a,p),S(e,p),S(s,p),S(i,p)}}}function Ta(_){let a,r,e,n,s,m,i,f,p,b,c,y,h,u,M,q,N,U;return a=new V({props:{id:"area_name",title:"Area"}}),e=new V({props:{id:"status_index",title:"Social status — mean ordinal class (mean rank; 1=least deprived … 4=most deprived), population-weighted where population data is complete and equal-weighted otherwise"}}),s=new V({props:{id:"dynamism_index",title:"Speed of change — mean ordinal class (mean rank; 3-year window), population-weighted where population data is complete and equal-weighted otherwise"}}),i=new V({props:{id:"stage_label",title:"Most widespread gentrification stage"}}),p=new V({props:{id:"dominant_share",title:"Most widespread stage's share (population-weighted, or equal-weighted — see the incomplete-data column)",fmt:"pct0"}}),c=new V({props:{id:"acute_stage_share",title:"Share in an acute-pressure stage (Active gentrification + Early pioneer signal + Improving, vulnerable area; population-weighted, or equal-weighted — see the incomplete-data column)",fmt:"pct0"}}),h=new V({props:{id:"n_habitable_children",title:"Gebiete with data"}}),M=new V({props:{id:"is_dominant_fragile",title:"Fragile (< 3 Gebiete)?"}}),N=new V({props:{id:"has_incomplete_population",title:"Population data incomplete (share above is equal-weighted, not population-weighted)?"}}),{c(){A(a.$$.fragment),r=k(),A(e.$$.fragment),n=k(),A(s.$$.fragment),m=k(),A(i.$$.fragment),f=k(),A(p.$$.fragment),b=k(),A(c.$$.fragment),y=k(),A(h.$$.fragment),u=k(),A(M.$$.fragment),q=k(),A(N.$$.fragment)},l(l){E(a.$$.fragment,l),r=$(l),E(e.$$.fragment,l),n=$(l),E(s.$$.fragment,l),m=$(l),E(i.$$.fragment,l),f=$(l),E(p.$$.fragment,l),b=$(l),E(c.$$.fragment,l),y=$(l),E(h.$$.fragment,l),u=$(l),E(M.$$.fragment,l),q=$(l),E(N.$$.fragment,l)},m(l,H){D(a,l,H),d(l,r,H),D(e,l,H),d(l,n,H),D(s,l,H),d(l,m,H),D(i,l,H),d(l,f,H),D(p,l,H),d(l,b,H),D(c,l,H),d(l,y,H),D(h,l,H),d(l,u,H),D(M,l,H),d(l,q,H),D(N,l,H),U=!0},p:X,i(l){U||(w(a.$$.fragment,l),w(e.$$.fragment,l),w(s.$$.fragment,l),w(i.$$.fragment,l),w(p.$$.fragment,l),w(c.$$.fragment,l),w(h.$$.fragment,l),w(M.$$.fragment,l),w(N.$$.fragment,l),U=!0)},o(l){v(a.$$.fragment,l),v(e.$$.fragment,l),v(s.$$.fragment,l),v(i.$$.fragment,l),v(p.$$.fragment,l),v(c.$$.fragment,l),v(h.$$.fragment,l),v(M.$$.fragment,l),v(N.$$.fragment,l),U=!1},d(l){l&&(o(r),o(n),o(m),o(f),o(b),o(y),o(u),o(q)),S(a,l),S(e,l),S(s,l),S(i,l),S(p,l),S(c,l),S(h,l),S(M,l),S(N,l)}}}function Ve(_){let a,r;return a=new Se({props:{queryID:"area_mix_table",queryResult:_[7]}}),{c(){A(a.$$.fragment)},l(e){E(a.$$.fragment,e)},m(e,n){D(a,e,n),r=!0},p(e,n){const s={};n[0]&128&&(s.queryResult=e[7]),a.$set(s)},i(e){r||(w(a.$$.fragment,e),r=!0)},o(e){v(a.$$.fragment,e),r=!1},d(e){S(a,e)}}}function Ba(_){let a,r,e,n,s,m,i,f,p,b;return a=new V({props:{id:"area_name",title:"Area"}}),e=new V({props:{id:"stage_label",title:"Stage"}}),s=new V({props:{id:"stage_population_share",title:"Share (population-weighted, or equal-weighted — see last column)",fmt:"pct0"}}),i=new V({props:{id:"stage_n_children",title:"Constituent Gebiete"}}),p=new V({props:{id:"has_incomplete_population",title:"Population data incomplete for this area?"}}),{c(){A(a.$$.fragment),r=k(),A(e.$$.fragment),n=k(),A(s.$$.fragment),m=k(),A(i.$$.fragment),f=k(),A(p.$$.fragment)},l(c){E(a.$$.fragment,c),r=$(c),E(e.$$.fragment,c),n=$(c),E(s.$$.fragment,c),m=$(c),E(i.$$.fragment,c),f=$(c),E(p.$$.fragment,c)},m(c,y){D(a,c,y),d(c,r,y),D(e,c,y),d(c,n,y),D(s,c,y),d(c,m,y),D(i,c,y),d(c,f,y),D(p,c,y),b=!0},p:X,i(c){b||(w(a.$$.fragment,c),w(e.$$.fragment,c),w(s.$$.fragment,c),w(i.$$.fragment,c),w(p.$$.fragment,c),b=!0)},o(c){v(a.$$.fragment,c),v(e.$$.fragment,c),v(s.$$.fragment,c),v(i.$$.fragment,c),v(p.$$.fragment,c),b=!1},d(c){c&&(o(r),o(n),o(m),o(f)),S(a,c),S(e,c),S(s,c),S(i,c),S(p,c)}}}function Ga(_){let a,r,e,n,s,m,i,f,p,b,c,y,h,u,M,q,N,U,l,H,ie,W,ne='<a href="#the-numbers-behind-the-map">The numbers behind the map</a>',K,Q,re,Y,x,B,Z,he=`Want the commercial-mix (shops/cafés) view instead of the social-status index — POI density and
Offering Advantage by domain — see the
<a href="/gentriduck/hamburg/poi-map" class="markdown">POI &amp; Offering Advantage map</a>.`,pe,se,ke='<a href="#honest-caveats">Honest caveats</a>',le,ee,ge=`<li class="markdown"><strong class="markdown">Social status and dynamism are ordinal, not linear</strong> — higher social-status shading means
<strong class="markdown">more deprived</strong>, not more prosperous. See <a href="/gentriduck/methodology" class="markdown">methodology &amp; data sources</a> for the
full decoder.</li> <li class="markdown"><strong class="markdown">Hamburg&#39;s Dynamik window is 3 years, Berlin&#39;s is 2</strong> — this is a qualitative difference in
what &quot;active-gentrification&quot; means, not just a numeric-scale one; see
<a href="/gentriduck/methodology" class="markdown">methodology §6</a> point 1.</li> <li class="markdown"><strong class="markdown">Stadtteil / Bezirk are population-weighted rollups, where possible, never a re-scored index.</strong>
The map colours by the <em class="markdown">most widespread</em> (&quot;dominant&quot;) stage among each area&#39;s constituent
Gebiete — always shown with its share (population-weighted where possible, equal-weighted as a
flagged fallback otherwise) and the full stage mix (see the &quot;Stage mix&quot; table), never as a
standalone label. Areas with fewer than 3 Gebiete contributing real data are flagged fragile.</li> <li class="markdown"><strong class="markdown">A coarse-grain reading is directionally biased toward &quot;calm&quot;, not neutral.</strong> Plurality voting
resolves an area to whichever stage is most widespread among its constituents, and the most
widespread stage is, by construction, usually the least acute one — so coarser levels
systematically understate pressure rather than randomly blurring it. An area shown as &quot;Stable,
established&quot; can still contain neighbourhoods under acute pressure; this map cannot be read as
evidence that pressure is absent anywhere in it. The share of the area in an acute-pressure stage
(residents where population data exists, otherwise constituent areas — in the tooltip and table
above) and the statistisches Gebiet level — the finest grain this site publishes — are where that
pressure is actually locatable.</li> <li class="markdown"><strong class="markdown">The rollup &quot;Social status&quot;/&quot;Speed of change&quot; figures are a mean ordinal class (a mean rank),
population-weighted where population data is complete and equal-weighted otherwise, not a
rescaled score.</strong> They are not offered as a map colour at Stadtteil/Bezirk grain, and the table
above no longer ranks by them, for exactly this reason — see the Indicator note above the map.</li> <li class="markdown"><strong class="markdown">&quot;Population-weighted&quot; is a best-effort weighting, not a guarantee.</strong> Hamburg&#39;s latest published
period has no population figures for effectively any Gebiet, so every Stadtteil/Bezirk rollup
shown here currently falls back to <em class="markdown">equal</em> weighting across its constituent Gebiete rather than
true population weighting — flagged per-area as &quot;population data incomplete&quot; in the table and
map tooltip.</li> <li class="markdown"><strong class="markdown">No neighbourhood names at the Gebiet level</strong> — Hamburg&#39;s statistische Gebiete are shown by
numeric code only; see the <a href="/gentriduck/hamburg" class="markdown">Hamburg data hub</a> for why. Stadtteil/Bezirk do have names.</li> <li class="markdown"><strong class="markdown">Only Hamburg&#39;s D1/D2 Sozialmonitoring outcome is shown here</strong> — the demographic (D4)
composite, land value &amp; rent, and Milieuschutz-equivalent displacement zones are not (yet)
published as a Hamburg map; see <a href="/gentriduck/methodology" class="markdown">methodology §6</a> for the full inventory.</li> <li class="markdown"><strong class="markdown">This map is never combined with, or compared numerically against, Berlin&#39;s map</strong> — see
<a href="/gentriduck/methodology" class="markdown">methodology §6</a>&#39;s structural rule against pooled cross-city ranking.</li>`,fe,_e,be,ae,oe,te=typeof G<"u"&&G.title&&G.hide_title!==!0&&ba();function qe(t,g){return typeof G<"u"&&G.title?ya:wa}let ce=qe()(_),de=typeof G=="object"&&va();m=new oa({props:{compact:!0,eyebrow:"Chapter 3 — The Evidence",title:"Maps — gentrification pressure by area",lede:"Colours each of Hamburg's statistische Gebiete by its current gentrification-pressure signal, from Hamburg's own official Sozialmonitoring — the same D1×D2 typology matrix used for Berlin, applied to Hamburg's own indicators and observation window."}}),f=new Me({props:{status:"info",$$slots:{default:[ka]},$$scope:{ctx:_}}}),b=new Me({props:{status:"warning",$$slots:{default:[Ha]},$$scope:{ctx:_}}}),y=new Ke({props:{name:"area_level",title:"Area level",defaultValue:"subarea_l2",$$slots:{default:[qa]},$$scope:{ctx:_}}});let J=_[2]&&Fe(_);const Ee=[Ea,Da],ue=[];function Ae(t,g){return t[2]?1:0}M=Ae(_),q=ue[M]=Ee[M](_);let C=_[3]&&We(_),z=_[4]&&Qe(_);H=new ga({props:{data:_[2]?_[4]:_[3],geoJsonUrl:_[9],geoId:"area_code",areaCol:"area_code",value:_[1]==="status_class"?"stage_label":_[1],legendType:_[1]==="status_class"?"categorical":"scalar",colorPalette:_[1]==="status_class"?_[11](_[2]?_[4]:_[3]):void 0,title:"Hamburg "+_[12][_[0].area_level.value]+" — "+_[10]+", latest period",startingLat:53.5511,startingLong:9.9937,startingZoom:10,tooltip:_[8]}});let j=_[5]&&Ye(_),P=_[6]&&Je(_);const we=[La,Ia],ye=[];function Ue(t,g){return t[2]?0:1}return Y=Ue(_),x=ye[Y]=we[Y](_),ae=new la({}),{c(){te&&te.c(),a=k(),ce.c(),r=L("meta"),e=L("meta"),de&&de.c(),n=Le(),s=k(),A(m.$$.fragment),i=k(),A(f.$$.fragment),p=k(),A(b.$$.fragment),c=k(),A(y.$$.fragment),h=k(),J&&J.c(),u=k(),q.c(),N=k(),C&&C.c(),U=k(),z&&z.c(),l=k(),A(H.$$.fragment),ie=k(),W=L("h2"),W.innerHTML=ne,K=k(),j&&j.c(),Q=k(),P&&P.c(),re=k(),x.c(),B=k(),Z=L("p"),Z.innerHTML=he,pe=k(),se=L("h2"),se.innerHTML=ke,le=k(),ee=L("ul"),ee.innerHTML=ge,fe=k(),_e=L("hr"),be=k(),A(ae.$$.fragment),this.h()},l(t){te&&te.l(t),a=$(t);const g=Ze("svelte-2igo1p",document.head);ce.l(g),r=I(g,"META",{name:!0,content:!0}),e=I(g,"META",{name:!0,content:!0}),de&&de.l(g),n=Le(),g.forEach(o),s=$(t),E(m.$$.fragment,t),i=$(t),E(f.$$.fragment,t),p=$(t),E(b.$$.fragment,t),c=$(t),E(y.$$.fragment,t),h=$(t),J&&J.l(t),u=$(t),q.l(t),N=$(t),C&&C.l(t),U=$(t),z&&z.l(t),l=$(t),E(H.$$.fragment,t),ie=$(t),W=I(t,"H2",{class:!0,id:!0,"data-svelte-h":!0}),R(W)!=="svelte-1loy64m"&&(W.innerHTML=ne),K=$(t),j&&j.l(t),Q=$(t),P&&P.l(t),re=$(t),x.l(t),B=$(t),Z=I(t,"P",{class:!0,"data-svelte-h":!0}),R(Z)!=="svelte-2hcun"&&(Z.innerHTML=he),pe=$(t),se=I(t,"H2",{class:!0,id:!0,"data-svelte-h":!0}),R(se)!=="svelte-ad0syq"&&(se.innerHTML=ke),le=$(t),ee=I(t,"UL",{class:!0,"data-svelte-h":!0}),R(ee)!=="svelte-yioox4"&&(ee.innerHTML=ge),fe=$(t),_e=I(t,"HR",{class:!0}),be=$(t),E(ae.$$.fragment,t),this.h()},h(){T(r,"name","twitter:card"),T(r,"content","summary_large_image"),T(e,"name","twitter:site"),T(e,"content","@evidence_dev"),T(W,"class","markdown"),T(W,"id","the-numbers-behind-the-map"),T(Z,"class","markdown"),T(se,"class","markdown"),T(se,"id","honest-caveats"),T(ee,"class","markdown"),T(_e,"class","markdown")},m(t,g){te&&te.m(t,g),d(t,a,g),ce.m(document.head,null),Ie(document.head,r),Ie(document.head,e),de&&de.m(document.head,null),Ie(document.head,n),d(t,s,g),D(m,t,g),d(t,i,g),D(f,t,g),d(t,p,g),D(b,t,g),d(t,c,g),D(y,t,g),d(t,h,g),J&&J.m(t,g),d(t,u,g),ue[M].m(t,g),d(t,N,g),C&&C.m(t,g),d(t,U,g),z&&z.m(t,g),d(t,l,g),D(H,t,g),d(t,ie,g),d(t,W,g),d(t,K,g),j&&j.m(t,g),d(t,Q,g),P&&P.m(t,g),d(t,re,g),ye[Y].m(t,g),d(t,B,g),d(t,Z,g),d(t,pe,g),d(t,se,g),d(t,le,g),d(t,ee,g),d(t,fe,g),d(t,_e,g),d(t,be,g),D(ae,t,g),oe=!0},p(t,g){typeof G<"u"&&G.title&&G.hide_title!==!0&&te.p(t,g),ce.p(t,g),typeof G=="object"&&de.p(t,g);const Ne={};g[1]&2097152&&(Ne.$$scope={dirty:g,ctx:t}),f.$set(Ne);const Re={};g[1]&2097152&&(Re.$$scope={dirty:g,ctx:t}),b.$set(Re);const ze={};g[1]&2097152&&(ze.$$scope={dirty:g,ctx:t}),y.$set(ze),t[2]?J?g[0]&4&&w(J,1):(J=Fe(t),J.c(),w(J,1),J.m(u.parentNode,u)):J&&($e(),v(J,1,1,()=>{J=null}),xe());let Te=M;M=Ae(t),M!==Te&&($e(),v(ue[Te],1,1,()=>{ue[Te]=null}),xe(),q=ue[M],q||(q=ue[M]=Ee[M](t),q.c()),w(q,1),q.m(N.parentNode,N)),t[3]?C?(C.p(t,g),g[0]&8&&w(C,1)):(C=We(t),C.c(),w(C,1),C.m(U.parentNode,U)):C&&($e(),v(C,1,1,()=>{C=null}),xe()),t[4]?z?(z.p(t,g),g[0]&16&&w(z,1)):(z=Qe(t),z.c(),w(z,1),z.m(l.parentNode,l)):z&&($e(),v(z,1,1,()=>{z=null}),xe());const ve={};g[0]&28&&(ve.data=t[2]?t[4]:t[3]),g[0]&512&&(ve.geoJsonUrl=t[9]),g[0]&2&&(ve.value=t[1]==="status_class"?"stage_label":t[1]),g[0]&2&&(ve.legendType=t[1]==="status_class"?"categorical":"scalar"),g[0]&30&&(ve.colorPalette=t[1]==="status_class"?t[11](t[2]?t[4]:t[3]):void 0),g[0]&1025&&(ve.title="Hamburg "+t[12][t[0].area_level.value]+" — "+t[10]+", latest period"),g[0]&256&&(ve.tooltip=t[8]),H.$set(ve),t[5]?j?(j.p(t,g),g[0]&32&&w(j,1)):(j=Ye(t),j.c(),w(j,1),j.m(Q.parentNode,Q)):j&&($e(),v(j,1,1,()=>{j=null}),xe()),t[6]?P?(P.p(t,g),g[0]&64&&w(P,1)):(P=Je(t),P.c(),w(P,1),P.m(re.parentNode,re)):P&&($e(),v(P,1,1,()=>{P=null}),xe());let Be=Y;Y=Ue(t),Y===Be?ye[Y].p(t,g):($e(),v(ye[Be],1,1,()=>{ye[Be]=null}),xe(),x=ye[Y],x?x.p(t,g):(x=ye[Y]=we[Y](t),x.c()),w(x,1),x.m(B.parentNode,B))},i(t){oe||(w(m.$$.fragment,t),w(f.$$.fragment,t),w(b.$$.fragment,t),w(y.$$.fragment,t),w(J),w(q),w(C),w(z),w(H.$$.fragment,t),w(j),w(P),w(x),w(ae.$$.fragment,t),oe=!0)},o(t){v(m.$$.fragment,t),v(f.$$.fragment,t),v(b.$$.fragment,t),v(y.$$.fragment,t),v(J),v(q),v(C),v(z),v(H.$$.fragment,t),v(j),v(P),v(x),v(ae.$$.fragment,t),oe=!1},d(t){t&&(o(a),o(s),o(i),o(p),o(c),o(h),o(u),o(N),o(U),o(l),o(ie),o(W),o(K),o(Q),o(re),o(B),o(Z),o(pe),o(se),o(le),o(ee),o(fe),o(_e),o(be)),te&&te.d(t),ce.d(t),o(r),o(e),de&&de.d(t),o(n),S(m,t),S(f,t),S(b,t),S(y,t),J&&J.d(t),ue[M].d(t),C&&C.d(t),z&&z.d(t),S(H,t),j&&j.d(t),P&&P.d(t),ye[Y].d(t),S(ae,t)}}}const G={title:"Maps — gentrification pressure by area"};function Ua(_,a,r){let e,n,s,m,i,f,p;Pe(_,pa,C=>r(35,f=C)),Pe(_,Oe,C=>r(40,p=C));let{data:b}=a,{data:c={},customFormattingSettings:y,__db:h,inputs:u}=b;ea(Oe,p="9f8718a8edaba5d4420c8c771a784d20",p);let M=_a(ma(u));aa(M.subscribe(C=>r(0,u=C))),ta(ha,{getCustomFormats:()=>y.customFormats||[]});const q=(C,z)=>fa(h.query,C,{query_name:z});ca(q),f.params,ia(()=>!0);let N={initialData:void 0,initialError:void 0},U=me`-- Only one (variant, area_level) combination exists for Hamburg's leaf grain in
-- gentrification_index: live_data / subarea_l2 (H3, #237). stage_label/stage_sort follow the
-- exact same D1xD2 typology-stage convention as /berlin/maps' \`areas_plr\` query (see that page's
-- header comment for the Dangschat 1988 / Döring & Ulbricht 2016 ordering rationale) -- same
-- matrix, reused unmodified per H1-domain-signoff.md §1.
select
    city_code,
    area_code,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by stage_sort`,l=`-- Only one (variant, area_level) combination exists for Hamburg's leaf grain in
-- gentrification_index: live_data / subarea_l2 (H3, #237). stage_label/stage_sort follow the
-- exact same D1xD2 typology-stage convention as /berlin/maps' \`areas_plr\` query (see that page's
-- header comment for the Dangschat 1988 / Döring & Ulbricht 2016 ordering rationale) -- same
-- matrix, reused unmodified per H1-domain-signoff.md §1.
select
    city_code,
    area_code,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by stage_sort`;c.areas_leaf_data&&(c.areas_leaf_data instanceof Error?N.initialError=c.areas_leaf_data:N.initialData=c.areas_leaf_data,c.areas_leaf_columns&&(N.knownColumns=c.areas_leaf_columns));let H,ie=!1;const W=Ce.createReactive({callback:C=>{r(3,H=C)},execFn:q},{id:"areas_leaf",...N});W(l,{noResolve:U,...N}),globalThis[Symbol.for("areas_leaf")]={get value(){return H}};let ne={initialData:void 0,initialError:void 0},K=me`-- #310: one row per rollup area (subarea_l1/district), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- see
-- /berlin/maps' \`areas_rollup\` query for the identical pattern/rationale (dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean,
-- n_habitable_children, is_dominant_fragile, has_incomplete_population are all CONSTANT across
-- an area's typology_stage rows).
--
-- #310 review fix (MEDIUM-A): \`area_table_rollup\`/\`area_mix_table\` below each re-run an
-- equivalent query directly against the mart rather than chaining from this block by name using
-- Evidence's own block-interpolation syntax -- not because Evidence/DuckDB-WASM lacks support for
-- that (an earlier revision of this comment claimed exactly that; it was wrong -- see
-- /berlin/maps' \`bezirk_names\` block, added in the #310 review's MEDIUM-A fix, for a working
-- example of one block referencing another this way), but because \`area_mix_table\` needs the full
-- per-stage grain this query has already collapsed via QUALIFY, and \`area_table_rollup\` only
-- needs a handful of the same columns -- a direct re-select reads just as clearly as a chained one
-- here, for a query this short. Hamburg's subarea_l1/district area_name is real (no
-- Berlin-Bezirk-style blank-name gap, see H3-domain-signoff.md §condition 3), so unlike
-- /berlin/maps this page has no name-fallback lookup to de-duplicate in the first place.
--
-- NB: this comment deliberately avoids spelling out this block's own name inside that
-- interpolation syntax -- Evidence's query-chaining resolver scans a block's full text (comments
-- included) for that exact token, so a block mentioning its own name that way, in its own text,
-- creates a literal self-reference the resolver reports as "Compiler error: circular reference"
-- (hit and fixed during this review pass, both here and in /berlin/maps' \`bezirk_names\` block).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' identical \`areas_rollup\` query for
-- the full rationale/citation and the NULL-vs-0 note (the mart is sparse, so a filtered SUM over
-- zero matching rows is NULL, not 0 -- \`acute_stage_share_raw\` is coalesced to 0 below only when
-- the area has at least one habitable child).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.city_code,
    m.area_code,
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    -- dominant_stage as status_class kept for shape symmetry with \`areas_leaf\`'s status_class
    -- column (#310 review, LOW-J) -- not otherwise consumed downstream at this grain (the map/
    -- tooltip read \`stage_label\` instead).
    m.dominant_stage as status_class,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case m.dominant_stage
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- #310 review fix (MEDIUM-4): pairs the most-widespread-stage reading with an explicit
    -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment point 3)
    -- this must never be shown as a standalone, confident-looking label. Blank ('') rather than
    -- NULL when not fragile, so the tooltip line renders empty instead of a formatted "-"
    -- placeholder.
    case
        when m.is_dominant_fragile
        then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
        else ''
    end as fragile_note,
    -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit caveat
    -- whenever the weighting silently degraded to equal-weight for this area (see
    -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / has_incomplete_population -- Hamburg's
    -- entire latest published period has this for effectively every Gebiet).
    case
        when m.has_incomplete_population
        then 'Population data incomplete for this area — equal-weighted, not population-weighted'
        else ''
    end as population_note
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
  -- defends against a future second variant silently duplicating rows here.
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by stage_sort`,Q=`-- #310: one row per rollup area (subarea_l1/district), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- see
-- /berlin/maps' \`areas_rollup\` query for the identical pattern/rationale (dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean,
-- n_habitable_children, is_dominant_fragile, has_incomplete_population are all CONSTANT across
-- an area's typology_stage rows).
--
-- #310 review fix (MEDIUM-A): \`area_table_rollup\`/\`area_mix_table\` below each re-run an
-- equivalent query directly against the mart rather than chaining from this block by name using
-- Evidence's own block-interpolation syntax -- not because Evidence/DuckDB-WASM lacks support for
-- that (an earlier revision of this comment claimed exactly that; it was wrong -- see
-- /berlin/maps' \`bezirk_names\` block, added in the #310 review's MEDIUM-A fix, for a working
-- example of one block referencing another this way), but because \`area_mix_table\` needs the full
-- per-stage grain this query has already collapsed via QUALIFY, and \`area_table_rollup\` only
-- needs a handful of the same columns -- a direct re-select reads just as clearly as a chained one
-- here, for a query this short. Hamburg's subarea_l1/district area_name is real (no
-- Berlin-Bezirk-style blank-name gap, see H3-domain-signoff.md §condition 3), so unlike
-- /berlin/maps this page has no name-fallback lookup to de-duplicate in the first place.
--
-- NB: this comment deliberately avoids spelling out this block's own name inside that
-- interpolation syntax -- Evidence's query-chaining resolver scans a block's full text (comments
-- included) for that exact token, so a block mentioning its own name that way, in its own text,
-- creates a literal self-reference the resolver reports as "Compiler error: circular reference"
-- (hit and fixed during this review pass, both here and in /berlin/maps' \`bezirk_names\` block).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' identical \`areas_rollup\` query for
-- the full rationale/citation and the NULL-vs-0 note (the mart is sparse, so a filtered SUM over
-- zero matching rows is NULL, not 0 -- \`acute_stage_share_raw\` is coalesced to 0 below only when
-- the area has at least one habitable child).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.city_code,
    m.area_code,
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    -- dominant_stage as status_class kept for shape symmetry with \`areas_leaf\`'s status_class
    -- column (#310 review, LOW-J) -- not otherwise consumed downstream at this grain (the map/
    -- tooltip read \`stage_label\` instead).
    m.dominant_stage as status_class,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case m.dominant_stage
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- #310 review fix (MEDIUM-4): pairs the most-widespread-stage reading with an explicit
    -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment point 3)
    -- this must never be shown as a standalone, confident-looking label. Blank ('') rather than
    -- NULL when not fragile, so the tooltip line renders empty instead of a formatted "-"
    -- placeholder.
    case
        when m.is_dominant_fragile
        then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
        else ''
    end as fragile_note,
    -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit caveat
    -- whenever the weighting silently degraded to equal-weight for this area (see
    -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / has_incomplete_population -- Hamburg's
    -- entire latest published period has this for effectively every Gebiet).
    case
        when m.has_incomplete_population
        then 'Population data incomplete for this area — equal-weighted, not population-weighted'
        else ''
    end as population_note
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
  -- defends against a future second variant silently duplicating rows here.
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by stage_sort`;c.areas_rollup_data&&(c.areas_rollup_data instanceof Error?ne.initialError=c.areas_rollup_data:ne.initialData=c.areas_rollup_data,c.areas_rollup_columns&&(ne.knownColumns=c.areas_rollup_columns));let re,Y=!1;const x=Ce.createReactive({callback:C=>{r(4,re=C)},execFn:q},{id:"areas_rollup",...ne});x(Q,{noResolve:K,...ne}),globalThis[Symbol.for("areas_rollup")]={get value(){return re}};let B={initialData:void 0,initialError:void 0},Z=me`select
    area_code,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by dynamism_index desc`,he=`select
    area_code,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by dynamism_index desc`;c.area_table_leaf_data&&(c.area_table_leaf_data instanceof Error?B.initialError=c.area_table_leaf_data:B.initialData=c.area_table_leaf_data,c.area_table_leaf_columns&&(B.knownColumns=c.area_table_leaf_columns));let pe,se=!1;const ke=Ce.createReactive({callback:C=>{r(5,pe=C)},execFn:q},{id:"area_table_leaf",...B});ke(he,{noResolve:Z,...B}),globalThis[Symbol.for("area_table_leaf")]={get value(){return pe}};let le={initialData:void 0,initialError:void 0},ee=me`-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' \`area_table_rollup\` query for the
-- full rationale/citation and the NULL-vs-0 note.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index_weighted_mean desc\`. Ranking a citywide table by a population-weighted
-- MEAN of an ordinal class code is exactly the "presented, coloured, or ordered" construct the
-- standing #267 domain decision forbids for a coarse scalar -- order by area_name instead (matches
-- /berlin/maps' equivalent fix and the Stage-mix table below). status_index/dynamism_index remain
-- visible as informational columns (see the Column titles below, per the geo-DS's C1: "mean
-- ordinal class (mean rank)", not a score -- #310 review fix D2-R2: population-weighted where
-- population data is complete and equal-weighted otherwise, not unconditionally).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`,ge=`-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' \`area_table_rollup\` query for the
-- full rationale/citation and the NULL-vs-0 note.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index_weighted_mean desc\`. Ranking a citywide table by a population-weighted
-- MEAN of an ordinal class code is exactly the "presented, coloured, or ordered" construct the
-- standing #267 domain decision forbids for a coarse scalar -- order by area_name instead (matches
-- /berlin/maps' equivalent fix and the Stage-mix table below). status_index/dynamism_index remain
-- visible as informational columns (see the Column titles below, per the geo-DS's C1: "mean
-- ordinal class (mean rank)", not a score -- #310 review fix D2-R2: population-weighted where
-- population data is complete and equal-weighted otherwise, not unconditionally).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`;c.area_table_rollup_data&&(c.area_table_rollup_data instanceof Error?le.initialError=c.area_table_rollup_data:le.initialData=c.area_table_rollup_data,c.area_table_rollup_columns&&(le.knownColumns=c.area_table_rollup_columns));let fe,_e=!1;const be=Ce.createReactive({callback:C=>{r(6,fe=C)},execFn:q},{id:"area_table_rollup",...le});be(ge,{noResolve:ee,...le}),globalThis[Symbol.for("area_table_rollup")]={get value(){return fe}};let ae={initialData:void 0,initialError:void 0},oe=me`-- #310 review fix (LOW-6): explicit variant filter, same as areas_rollup/area_table_rollup above
-- -- Hamburg's subarea_l1/district area_name is real (no Berlin-Bezirk-style blank-name gap), so
-- unlike /berlin/maps' equivalent query this one needs no name-fallback join.
--
-- #310 review fix (R-310-1): stage_label de-jargons typology_stage with the exact same stage-name
-- mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level.
select
    area_name,
    case typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    stage_population_share,
    stage_n_children,
    has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix
where area_level = '${u.area_level.value}'
  and city_code = 'HH'
  and variant = 'live_data'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
order by area_name, stage_population_share desc nulls last`,te=`-- #310 review fix (LOW-6): explicit variant filter, same as areas_rollup/area_table_rollup above
-- -- Hamburg's subarea_l1/district area_name is real (no Berlin-Bezirk-style blank-name gap), so
-- unlike /berlin/maps' equivalent query this one needs no name-fallback join.
--
-- #310 review fix (R-310-1): stage_label de-jargons typology_stage with the exact same stage-name
-- mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level.
select
    area_name,
    case typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    stage_population_share,
    stage_n_children,
    has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix
where area_level = '${u.area_level.value}'
  and city_code = 'HH'
  and variant = 'live_data'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
order by area_name, stage_population_share desc nulls last`;c.area_mix_table_data&&(c.area_mix_table_data instanceof Error?ae.initialError=c.area_mix_table_data:ae.initialData=c.area_mix_table_data,c.area_mix_table_columns&&(ae.knownColumns=c.area_mix_table_columns));let qe,De=!1;const ce=Ce.createReactive({callback:C=>{r(7,qe=C)},execFn:q},{id:"area_mix_table",...ae});ce(te,{noResolve:oe,...ae}),globalThis[Symbol.for("area_mix_table")]={get value(){return qe}};const de={"Active gentrification":"#d73027","Early pioneer signal":"#fc8d59","Improving, vulnerable area":"#fee090","Pre-gentrification watch":"#e0f3f8","Consolidated, still intensifying":"#91bfdb","Stable, established":"#4575b4"};function J(C){const z=new Set,j=[];for(const P of C??[]){const we=P==null?void 0:P.stage_label;we!=null&&!z.has(we)&&we in de&&(z.add(we),j.push(de[we]))}return j}const Ee={status_class:"Gentrification stage",status_index:"Social status",dynamism_index:"Dynamism"},ue={subarea_l2:"subarea_l2_live_data.geojson",subarea_l1:"subarea_l1_current.geojson",district:"district_current.geojson"},Ae={subarea_l2:"statistisches Gebiet",subarea_l1:"Stadtteil",district:"Bezirk"};return _.$$set=C=>{"data"in C&&r(13,b=C.data)},_.$$.update=()=>{_.$$.dirty[0]&8192&&r(14,{data:c={},customFormattingSettings:y,__db:h}=b,c),_.$$.dirty[0]&16384&&ua.set(Object.keys(c).length>0),_.$$.dirty[1]&16&&f.params,_.$$.dirty[0]&491520&&(U||!ie?U||(W(l,{noResolve:U,...N}),r(18,ie=!0)):W(l,{noResolve:U})),_.$$.dirty[0]&1&&r(20,K=me`-- #310: one row per rollup area (subarea_l1/district), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- see
-- /berlin/maps' \`areas_rollup\` query for the identical pattern/rationale (dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean,
-- n_habitable_children, is_dominant_fragile, has_incomplete_population are all CONSTANT across
-- an area's typology_stage rows).
--
-- #310 review fix (MEDIUM-A): \`area_table_rollup\`/\`area_mix_table\` below each re-run an
-- equivalent query directly against the mart rather than chaining from this block by name using
-- Evidence's own block-interpolation syntax -- not because Evidence/DuckDB-WASM lacks support for
-- that (an earlier revision of this comment claimed exactly that; it was wrong -- see
-- /berlin/maps' \`bezirk_names\` block, added in the #310 review's MEDIUM-A fix, for a working
-- example of one block referencing another this way), but because \`area_mix_table\` needs the full
-- per-stage grain this query has already collapsed via QUALIFY, and \`area_table_rollup\` only
-- needs a handful of the same columns -- a direct re-select reads just as clearly as a chained one
-- here, for a query this short. Hamburg's subarea_l1/district area_name is real (no
-- Berlin-Bezirk-style blank-name gap, see H3-domain-signoff.md §condition 3), so unlike
-- /berlin/maps this page has no name-fallback lookup to de-duplicate in the first place.
--
-- NB: this comment deliberately avoids spelling out this block's own name inside that
-- interpolation syntax -- Evidence's query-chaining resolver scans a block's full text (comments
-- included) for that exact token, so a block mentioning its own name that way, in its own text,
-- creates a literal self-reference the resolver reports as "Compiler error: circular reference"
-- (hit and fixed during this review pass, both here and in /berlin/maps' \`bezirk_names\` block).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' identical \`areas_rollup\` query for
-- the full rationale/citation and the NULL-vs-0 note (the mart is sparse, so a filtered SUM over
-- zero matching rows is NULL, not 0 -- \`acute_stage_share_raw\` is coalesced to 0 below only when
-- the area has at least one habitable child).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.city_code,
    m.area_code,
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    -- dominant_stage as status_class kept for shape symmetry with \`areas_leaf\`'s status_class
    -- column (#310 review, LOW-J) -- not otherwise consumed downstream at this grain (the map/
    -- tooltip read \`stage_label\` instead).
    m.dominant_stage as status_class,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case m.dominant_stage
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- #310 review fix (MEDIUM-4): pairs the most-widespread-stage reading with an explicit
    -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment point 3)
    -- this must never be shown as a standalone, confident-looking label. Blank ('') rather than
    -- NULL when not fragile, so the tooltip line renders empty instead of a formatted "-"
    -- placeholder.
    case
        when m.is_dominant_fragile
        then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
        else ''
    end as fragile_note,
    -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit caveat
    -- whenever the weighting silently degraded to equal-weight for this area (see
    -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / has_incomplete_population -- Hamburg's
    -- entire latest published period has this for effectively every Gebiet).
    case
        when m.has_incomplete_population
        then 'Population data incomplete for this area — equal-weighted, not population-weighted'
        else ''
    end as population_note
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
  -- defends against a future second variant silently duplicating rows here.
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by stage_sort`),_.$$.dirty[0]&1&&r(21,Q=`-- #310: one row per rollup area (subarea_l1/district), picked deterministically from
-- mart_area_rollup_stage_mix's per-(area, typology_stage) grain via QUALIFY -- see
-- /berlin/maps' \`areas_rollup\` query for the identical pattern/rationale (dominant_stage,
-- dominant_share, status_index_weighted_mean, dynamism_index_weighted_mean,
-- n_habitable_children, is_dominant_fragile, has_incomplete_population are all CONSTANT across
-- an area's typology_stage rows).
--
-- #310 review fix (MEDIUM-A): \`area_table_rollup\`/\`area_mix_table\` below each re-run an
-- equivalent query directly against the mart rather than chaining from this block by name using
-- Evidence's own block-interpolation syntax -- not because Evidence/DuckDB-WASM lacks support for
-- that (an earlier revision of this comment claimed exactly that; it was wrong -- see
-- /berlin/maps' \`bezirk_names\` block, added in the #310 review's MEDIUM-A fix, for a working
-- example of one block referencing another this way), but because \`area_mix_table\` needs the full
-- per-stage grain this query has already collapsed via QUALIFY, and \`area_table_rollup\` only
-- needs a handful of the same columns -- a direct re-select reads just as clearly as a chained one
-- here, for a query this short. Hamburg's subarea_l1/district area_name is real (no
-- Berlin-Bezirk-style blank-name gap, see H3-domain-signoff.md §condition 3), so unlike
-- /berlin/maps this page has no name-fallback lookup to de-duplicate in the first place.
--
-- NB: this comment deliberately avoids spelling out this block's own name inside that
-- interpolation syntax -- Evidence's query-chaining resolver scans a block's full text (comments
-- included) for that exact token, so a block mentioning its own name that way, in its own text,
-- creates a literal self-reference the resolver reports as "Compiler error: circular reference"
-- (hit and fixed during this review pass, both here and in /berlin/maps' \`bezirk_names\` block).
--
-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' identical \`areas_rollup\` query for
-- the full rationale/citation and the NULL-vs-0 note (the mart is sparse, so a filtered SUM over
-- zero matching rows is NULL, not 0 -- \`acute_stage_share_raw\` is coalesced to 0 below only when
-- the area has at least one habitable child).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.city_code,
    m.area_code,
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    -- dominant_stage as status_class kept for shape symmetry with \`areas_leaf\`'s status_class
    -- column (#310 review, LOW-J) -- not otherwise consumed downstream at this grain (the map/
    -- tooltip read \`stage_label\` instead).
    m.dominant_stage as status_class,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case m.dominant_stage
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort,
    -- #310 review fix (MEDIUM-4): pairs the most-widespread-stage reading with an explicit
    -- caveat whenever is_dominant_fragile -- per the #310 design decision (issue comment point 3)
    -- this must never be shown as a standalone, confident-looking label. Blank ('') rather than
    -- NULL when not fragile, so the tooltip line renders empty instead of a formatted "-"
    -- placeholder.
    case
        when m.is_dominant_fragile
        then 'Small sample — fewer than 3 constituent areas, most-widespread-stage reading may not be robust'
        else ''
    end as fragile_note,
    -- #310 review fix (MEDIUM-3): pairs the "population-weighted" claim with an explicit caveat
    -- whenever the weighting silently degraded to equal-weight for this area (see
    -- mart_area_rollup_stage_mix's own WEIGHTING NOTE / has_incomplete_population -- Hamburg's
    -- entire latest published period has this for effectively every Gebiet).
    case
        when m.has_incomplete_population
        then 'Population data incomplete for this area — equal-weighted, not population-weighted'
        else ''
    end as population_note
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  -- #310 review fix (LOW-6): explicit even though the mart is currently single-variant --
  -- defends against a future second variant silently duplicating rows here.
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by stage_sort`),_.$$.dirty[0]&7864320&&(K||!Y?K||(x(Q,{noResolve:K,...ne}),r(22,Y=!0)):x(Q,{noResolve:K})),_.$$.dirty[0]&125829120&&(Z||!se?Z||(ke(he,{noResolve:Z,...B}),r(26,se=!0)):ke(he,{noResolve:Z})),_.$$.dirty[0]&1&&r(28,ee=me`-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' \`area_table_rollup\` query for the
-- full rationale/citation and the NULL-vs-0 note.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index_weighted_mean desc\`. Ranking a citywide table by a population-weighted
-- MEAN of an ordinal class code is exactly the "presented, coloured, or ordered" construct the
-- standing #267 domain decision forbids for a coarse scalar -- order by area_name instead (matches
-- /berlin/maps' equivalent fix and the Stage-mix table below). status_index/dynamism_index remain
-- visible as informational columns (see the Column titles below, per the geo-DS's C1: "mean
-- ordinal class (mean rank)", not a score -- #310 review fix D2-R2: population-weighted where
-- population data is complete and equal-weighted otherwise, not unconditionally).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`),_.$$.dirty[0]&1&&r(29,ge=`-- #310 review fix (D4 -- domain sign-off blocking): \`acute\` sums stage_population_share over the
-- three most acute typology_stage values -- see /berlin/maps' \`area_table_rollup\` query for the
-- full rationale/citation and the NULL-vs-0 note.
--
-- #310 review fix (D1/C1 -- domain sign-off section (b)): this table no longer
-- \`order by dynamism_index_weighted_mean desc\`. Ranking a citywide table by a population-weighted
-- MEAN of an ordinal class code is exactly the "presented, coloured, or ordered" construct the
-- standing #267 domain decision forbids for a coarse scalar -- order by area_name instead (matches
-- /berlin/maps' equivalent fix and the Stage-mix table below). status_index/dynamism_index remain
-- visible as informational columns (see the Column titles below, per the geo-DS's C1: "mean
-- ordinal class (mean rank)", not a score -- #310 review fix D2-R2: population-weighted where
-- population data is complete and equal-weighted otherwise, not unconditionally).
with
    acute as (
        select
            area_code,
            sum(stage_population_share) filter (
                where typology_stage in
                    ('active-gentrification', 'pioneer-signal', 'improving-vulnerable')
            ) as acute_stage_share_raw
        from gentriduck_marts.mart_area_rollup_stage_mix
        where area_level = '${u.area_level.value}'
          and city_code = 'HH'
          and variant = 'live_data'
          and period_yyyymm = (
              select max(period_yyyymm)
              from gentriduck_marts.mart_area_rollup_stage_mix
              where
                  area_level = '${u.area_level.value}' and city_code = 'HH'
                  and variant = 'live_data'
          )
        group by area_code
    )
select
    m.area_name,
    m.status_index_weighted_mean as status_index,
    m.dynamism_index_weighted_mean as dynamism_index,
    case m.dominant_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    m.dominant_share,
    case
        when m.n_habitable_children = 0 then null
        else coalesce(a.acute_stage_share_raw, 0)
    end as acute_stage_share,
    m.n_habitable_children,
    m.is_dominant_fragile,
    m.has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix as m
left join acute as a on m.area_code = a.area_code
where m.area_level = '${u.area_level.value}'
  and m.city_code = 'HH'
  and m.variant = 'live_data'
  and m.period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
qualify row_number() over (partition by m.area_code order by m.typology_stage) = 1
order by area_name`),_.$$.dirty[0]&2013265920&&(ee||!_e?ee||(be(ge,{noResolve:ee,...le}),r(30,_e=!0)):be(ge,{noResolve:ee})),_.$$.dirty[0]&1&&r(32,oe=me`-- #310 review fix (LOW-6): explicit variant filter, same as areas_rollup/area_table_rollup above
-- -- Hamburg's subarea_l1/district area_name is real (no Berlin-Bezirk-style blank-name gap), so
-- unlike /berlin/maps' equivalent query this one needs no name-fallback join.
--
-- #310 review fix (R-310-1): stage_label de-jargons typology_stage with the exact same stage-name
-- mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level.
select
    area_name,
    case typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    stage_population_share,
    stage_n_children,
    has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix
where area_level = '${u.area_level.value}'
  and city_code = 'HH'
  and variant = 'live_data'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
order by area_name, stage_population_share desc nulls last`),_.$$.dirty[0]&1&&r(33,te=`-- #310 review fix (LOW-6): explicit variant filter, same as areas_rollup/area_table_rollup above
-- -- Hamburg's subarea_l1/district area_name is real (no Berlin-Bezirk-style blank-name gap), so
-- unlike /berlin/maps' equivalent query this one needs no name-fallback join.
--
-- #310 review fix (R-310-1): stage_label de-jargons typology_stage with the exact same stage-name
-- mapping used everywhere else on this page -- the raw machine code (e.g.
-- 'active-gentrification') was previously shown verbatim here, the only table on the page that
-- did so. has_incomplete_population is added (D2) so this table -- unlike area_table_rollup above,
-- which has one row per area -- can disclose per-row whether THIS area's shares are population- or
-- equal-weighted, since a single mix table spans every area at the selected level.
select
    area_name,
    case typology_stage
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
        else typology_stage -- 'uninhabited / no data' -- already plain language, pass through
    end as stage_label,
    stage_population_share,
    stage_n_children,
    has_incomplete_population
from gentriduck_marts.mart_area_rollup_stage_mix
where area_level = '${u.area_level.value}'
  and city_code = 'HH'
  and variant = 'live_data'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.mart_area_rollup_stage_mix
      where area_level = '${u.area_level.value}' and city_code = 'HH' and variant = 'live_data'
  )
order by area_name, stage_population_share desc nulls last`),_.$$.dirty[1]&15&&(oe||!De?oe||(ce(te,{noResolve:oe,...ae}),r(34,De=!0)):ce(te,{noResolve:oe})),_.$$.dirty[0]&1&&r(2,e=u.area_level.value!=="subarea_l2"),_.$$.dirty[0]&5&&r(1,n=e?"status_class":u.indicator.value),_.$$.dirty[0]&5&&r(10,s=e?"Gentrification stage — plain-language, colour-coded":u.indicator.label),_.$$.dirty[0]&1&&r(9,m=`${da}/geo/${ue[u.area_level.value]}`),_.$$.dirty[0]&6&&r(8,i=e?[{id:"area_name",showColumnName:!1,valueClass:"font-bold text-sm",fmt:"id"},{id:"stage_label",title:"Most widespread stage",fmt:"id"},{id:"dominant_share",title:"Most widespread stage's share",fmt:"pct0"},{id:"acute_stage_share",title:"Share in an acute-pressure stage",fmt:"pct0"},{id:"n_habitable_children",title:"Constituent Gebiete with data",fmt:"id"},{id:"fragile_note",showColumnName:!1,valueClass:"text-xs text-amber-700",fmt:"id"},{id:"population_note",showColumnName:!1,valueClass:"text-xs text-amber-700",fmt:"id"},{id:"area_code",title:"Area code",valueClass:"text-xs opacity-60",fmt:"id"}]:[{id:n==="status_class"?"stage_label":n,title:Ee[n],fmt:n==="status_class"?"id":"num1"},{id:"area_code",title:"Gebiet code",valueClass:"text-xs opacity-60",fmt:"id"}])},r(16,U=me`-- Only one (variant, area_level) combination exists for Hamburg's leaf grain in
-- gentrification_index: live_data / subarea_l2 (H3, #237). stage_label/stage_sort follow the
-- exact same D1xD2 typology-stage convention as /berlin/maps' \`areas_plr\` query (see that page's
-- header comment for the Dangschat 1988 / Döring & Ulbricht 2016 ordering rationale) -- same
-- matrix, reused unmodified per H1-domain-signoff.md §1.
select
    city_code,
    area_code,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by stage_sort`),r(17,l=`-- Only one (variant, area_level) combination exists for Hamburg's leaf grain in
-- gentrification_index: live_data / subarea_l2 (H3, #237). stage_label/stage_sort follow the
-- exact same D1xD2 typology-stage convention as /berlin/maps' \`areas_plr\` query (see that page's
-- header comment for the Dangschat 1988 / Döring & Ulbricht 2016 ordering rationale) -- same
-- matrix, reused unmodified per H1-domain-signoff.md §1.
select
    city_code,
    area_code,
    status_index,
    dynamism_index,
    status_class,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label,
    case status_class
        when 'active-gentrification' then 1
        when 'pioneer-signal' then 2
        when 'improving-vulnerable' then 3
        when 'pre-gentrification' then 4
        when 'consolidation-pressure' then 5
        when 'stable-established' then 6
        else 99
    end as stage_sort
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by stage_sort`),r(24,Z=me`select
    area_code,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by dynamism_index desc`),r(25,he=`select
    area_code,
    status_index,
    dynamism_index,
    case status_class
        when 'active-gentrification' then 'Active gentrification'
        when 'pioneer-signal' then 'Early pioneer signal'
        when 'improving-vulnerable' then 'Improving, vulnerable area'
        when 'pre-gentrification' then 'Pre-gentrification watch'
        when 'consolidation-pressure' then 'Consolidated, still intensifying'
        when 'stable-established' then 'Stable, established'
    end as stage_label
from gentriduck_marts.gentrification_index
where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  and period_yyyymm = (
      select max(period_yyyymm)
      from gentriduck_marts.gentrification_index
      where variant = 'live_data' and area_level = 'subarea_l2' and city_code = 'HH'
  )
order by dynamism_index desc`),[u,n,e,H,re,pe,fe,qe,i,m,s,J,Ae,b,c,N,U,l,ie,ne,K,Q,Y,B,Z,he,se,le,ee,ge,_e,ae,oe,te,De,f]}class Za extends ra{constructor(a){super(),sa(this,a,Ua,Ga,Xe,{data:13},null,[-1,-1])}}export{Za as component};
