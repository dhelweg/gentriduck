# Normalize line endings across macOS / Windows / Linux checkouts.
* text=auto eol=lf

# Treat known binary/data types as binary (no EOL munging, no diff noise).
*.duckdb binary
*.parquet binary
*.pbf binary
*.xlsx binary
*.png binary
*.jpg binary
