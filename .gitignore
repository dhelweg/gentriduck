# --- Python / uv ---
.venv/
__pycache__/
*.py[cod]
.python-version.local

# --- Secrets / env ---
.env
.env.*
!.env.example

# --- DuckDB / large + raw data (rebuilt from open sources, never committed) ---
*.duckdb
*.duckdb.wal
data/raw/
data/cache/
*.osm.pbf
*.osh.pbf
*.parquet

# --- dbt ---
transform/target/
transform/dbt_packages/
transform/logs/
logs/
.user.yml

# --- web (later) ---
web/node_modules/
web/.svelte-kit/
web/build/

# --- local session transcripts (kept on-machine for continuity; never published) ---
.sessions/

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
